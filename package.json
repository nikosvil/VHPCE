{
  "name": "vhpce",
  "private": true,
  "version": "0.0.0",
  "description": "Visual HPC for Engineers — interactive performance laboratory",
  "packageManager": "pnpm@11.5.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "gateway:build:cpu": "docker compose -f infra/docker/compose.yml --profile build build bench runner mpi",
    "gateway:build:gpu": "docker compose -f infra/docker/compose.yml --profile build build cuda",
    "gateway:up": "docker compose -f infra/docker/compose.yml up -d",
    "gateway:down": "docker compose -f infra/docker/compose.yml down",
    "gateway:logs": "docker compose -f infra/docker/compose.yml logs -f"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.6.0"
  }
}
