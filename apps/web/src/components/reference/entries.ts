// The /reference command library. Each entry maps to an archetype animation (archetypes.ts)
// + params, so coverage grows by adding data, not code. `exampleId` (when set) links to a
// runnable Playground example. OpenACC entries are a planned follow-up (the Tech type allows it).
import type { ArchetypeParams } from "./archetypes";

export type Tech = "OpenMP" | "MPI" | "OpenACC" | "Slurm" | "pthreads" | "CUDA";

export type RefEntry = {
  id: string;
  tech: Tech;
  category: string;
  name: string;        // the directive / call as written
  signature: string;   // canonical syntax (C / C++)
  signatureF?: string; // explicit Fortran form (else derived by toFortran)
  summary: string;     // one-sentence plain-English meaning
  note?: string;       // short "good to know" / when-to-use / gotcha
  visual: { archetype: string; params?: ArchetypeParams };
  exampleId?: string;  // a Playground example id (-> /playground?ex=...)
  related?: string[];  // other entry ids
};

export const ENTRIES: RefEntry[] = [
  /* ===================== OpenMP ===================== */
  {
    id: "omp_parallel", tech: "OpenMP", category: "Parallel regions", name: "#pragma omp parallel",
    signature: "#pragma omp parallel [clauses]\n{ /* each thread runs this */ }",
    summary: "Forks a team of threads that all run the enclosed block; they join at the end.",
    note: "By itself every thread runs the WHOLE block — add a work-sharing construct (for/sections) to split work.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, exampleId: "hello",
    related: ["omp_for", "omp_get_thread_num"],
  },
  {
    id: "omp_get_thread_num", tech: "OpenMP", category: "Parallel regions", name: "omp_get_thread_num()",
    signature: "int id = omp_get_thread_num();",
    summary: "Returns the calling thread's id within its team (0 .. num_threads-1).",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, exampleId: "hello", related: ["omp_get_num_threads", "omp_parallel"],
  },
  {
    id: "omp_get_num_threads", tech: "OpenMP", category: "Parallel regions", name: "omp_get_num_threads()",
    signature: "int n = omp_get_num_threads();",
    summary: "Returns how many threads are in the current team.",
    note: "Outside a parallel region it returns 1. For the max, use omp_get_max_threads().",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, exampleId: "hello", related: ["omp_get_thread_num"],
  },
  {
    id: "omp_set_num_threads", tech: "OpenMP", category: "Parallel regions", name: "omp_set_num_threads(n)",
    signature: "omp_set_num_threads(8);",
    summary: "Sets how many threads the next parallel region should use.",
    note: "Or set it per-region with `num_threads(n)`, or globally with the OMP_NUM_THREADS env var.",
    visual: { archetype: "forkJoin", params: { threads: 8 } }, related: ["env_omp_num_threads"],
  },
  {
    id: "env_omp_num_threads", tech: "OpenMP", category: "Parallel regions", name: "OMP_NUM_THREADS",
    signature: "export OMP_NUM_THREADS=8   # environment variable",
    summary: "Environment variable choosing the default team size — no recompile needed.",
    visual: { archetype: "forkJoin", params: { threads: 8 } }, related: ["omp_set_num_threads"],
  },
  {
    id: "omp_parallel_for", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp parallel for",
    signature: "#pragma omp parallel for\nfor (i = 0; i < N; i++) work(i);",
    summary: "Shorthand for a parallel region + a for-construct: splits the loop across threads.",
    note: "The most common OpenMP line. Loop iterations must be independent.",
    visual: { archetype: "worksharing", params: { kind: "for", threads: 4 } }, exampleId: "reduction",
    related: ["omp_for", "omp_parallel"],
  },
  {
    id: "omp_for", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp for",
    signature: "#pragma omp parallel\n{ #pragma omp for\n  for (...) work(i); }",
    summary: "Inside a parallel region, divides the loop's iterations among the existing team.",
    visual: { archetype: "worksharing", params: { kind: "for", threads: 4 } }, exampleId: "reduction",
    related: ["omp_parallel_for", "omp_schedule_static"],
  },
  {
    id: "omp_sections", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp sections",
    signature: "#pragma omp sections\n{ #pragma omp section\n  taskA();\n  #pragma omp section\n  taskB(); }",
    summary: "Runs each independent code section once, on a different thread (functional parallelism).",
    visual: { archetype: "worksharing", params: { kind: "sections" } }, related: ["omp_for", "omp_task"],
  },
  {
    id: "omp_single", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp single",
    signature: "#pragma omp single\n{ /* run by exactly one thread */ }",
    summary: "Exactly one thread (any one) runs the block; the others skip it.",
    note: "There's an implicit barrier at the end (use `nowait` to drop it).",
    visual: { archetype: "worksharing", params: { kind: "single" } }, related: ["omp_master", "omp_barrier"],
  },
  {
    id: "omp_master", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp master",
    signature: "#pragma omp master\n{ /* run only by thread 0 */ }",
    summary: "Only the master thread (id 0) runs the block; no implicit barrier.",
    visual: { archetype: "worksharing", params: { kind: "master" } }, related: ["omp_single"],
  },
  {
    id: "omp_schedule_static", tech: "OpenMP", category: "Scheduling", name: "schedule(static)",
    signature: "#pragma omp for schedule(static[,chunk])",
    summary: "Splits iterations into equal contiguous chunks decided up front — lowest overhead.",
    note: "Best when every iteration costs about the same.",
    visual: { archetype: "schedule", params: { kind: "static" } }, exampleId: "schedule", related: ["omp_schedule_dynamic", "omp_schedule_guided"],
  },
  {
    id: "omp_schedule_dynamic", tech: "OpenMP", category: "Scheduling", name: "schedule(dynamic)",
    signature: "#pragma omp for schedule(dynamic[,chunk])",
    summary: "Threads grab small chunks on demand as they finish — balances uneven work.",
    note: "Fixes load imbalance, but the hand-out has overhead; tune the chunk size.",
    visual: { archetype: "schedule", params: { kind: "dynamic" } }, exampleId: "schedule", related: ["omp_schedule_static", "omp_schedule_guided"],
  },
  {
    id: "omp_schedule_guided", tech: "OpenMP", category: "Scheduling", name: "schedule(guided)",
    signature: "#pragma omp for schedule(guided[,chunk])",
    summary: "Like dynamic but chunks start large and shrink — good balance with less overhead.",
    visual: { archetype: "schedule", params: { kind: "guided" } }, exampleId: "schedule", related: ["omp_schedule_dynamic"],
  },
  {
    id: "omp_collapse", tech: "OpenMP", category: "Scheduling", name: "collapse(n)",
    signature: "#pragma omp for collapse(2)\nfor (i..) for (j..) work(i,j);",
    summary: "Merges n perfectly-nested loops into one larger iteration space to parallelize.",
    note: "Use when the outer loop alone has too few iterations to fill the threads.",
    visual: { archetype: "worksharing", params: { kind: "for", threads: 4 } }, exampleId: "collapse", related: ["omp_parallel_for"],
  },
  {
    id: "omp_barrier", tech: "OpenMP", category: "Synchronization", name: "#pragma omp barrier",
    signature: "#pragma omp barrier",
    summary: "Every thread waits here until all threads in the team arrive.",
    visual: { archetype: "barrier", params: { kind: "barrier" } }, related: ["omp_nowait", "omp_taskwait"],
  },
  {
    id: "omp_nowait", tech: "OpenMP", category: "Synchronization", name: "nowait",
    signature: "#pragma omp for nowait",
    summary: "Removes the implicit barrier at the end of a work-sharing construct.",
    note: "Speeds things up, but only safe when later code doesn't depend on all iterations finishing.",
    visual: { archetype: "barrier", params: { kind: "nowait" } }, related: ["omp_barrier"],
  },
  {
    id: "omp_critical", tech: "OpenMP", category: "Synchronization", name: "#pragma omp critical",
    signature: "#pragma omp critical\n{ /* one thread at a time */ }",
    summary: "Makes the block mutually exclusive — only one thread executes it at a time.",
    note: "Correct but slow if hot: it serializes. Prefer reduction/atomic where possible.",
    visual: { archetype: "criticalAtomic", params: { kind: "critical" } }, exampleId: "sync", related: ["omp_atomic", "omp_reduction"],
  },
  {
    id: "omp_atomic", tech: "OpenMP", category: "Synchronization", name: "#pragma omp atomic",
    signature: "#pragma omp atomic\nsum += x;",
    summary: "A single memory update done atomically — cheaper than critical for one operation.",
    visual: { archetype: "criticalAtomic", params: { kind: "atomic" } }, exampleId: "sync", related: ["omp_critical", "omp_reduction"],
  },
  {
    id: "omp_locks", tech: "OpenMP", category: "Synchronization", name: "omp_set_lock / omp_unset_lock",
    signature: "omp_lock_t l; omp_init_lock(&l);\nomp_set_lock(&l); /* ... */ omp_unset_lock(&l);",
    summary: "Explicit locks for fine-grained mutual exclusion when critical isn't flexible enough.",
    visual: { archetype: "criticalAtomic", params: { kind: "lock" } }, related: ["omp_critical"],
  },
  {
    id: "omp_taskwait", tech: "OpenMP", category: "Synchronization", name: "#pragma omp taskwait",
    signature: "#pragma omp taskwait",
    summary: "Waits for the child tasks spawned by the current task to complete.",
    visual: { archetype: "barrier", params: { kind: "barrier" } }, exampleId: "tasks", related: ["omp_task"],
  },
  {
    id: "omp_shared", tech: "OpenMP", category: "Data sharing", name: "shared(var)",
    signature: "#pragma omp parallel shared(x)",
    summary: "All threads access the SAME variable — one copy in shared memory.",
    note: "Concurrent writes to a shared variable are a data race; guard them (reduction/atomic/critical).",
    visual: { archetype: "dataSharing", params: { clause: "shared" } }, exampleId: "falsesharing",
    related: ["omp_private", "omp_reduction"],
  },
  {
    id: "omp_private", tech: "OpenMP", category: "Data sharing", name: "private(var)",
    signature: "#pragma omp parallel private(x)",
    summary: "Each thread gets its own uninitialised copy of the variable.",
    visual: { archetype: "dataSharing", params: { clause: "private" } }, exampleId: "histogram", related: ["omp_firstprivate", "omp_shared"],
  },
  {
    id: "omp_firstprivate", tech: "OpenMP", category: "Data sharing", name: "firstprivate(var)",
    signature: "#pragma omp parallel firstprivate(x)",
    summary: "Private copies, each initialised from the variable's value before the region.",
    visual: { archetype: "dataSharing", params: { clause: "firstprivate" } }, exampleId: "histogram", related: ["omp_private", "omp_lastprivate"],
  },
  {
    id: "omp_lastprivate", tech: "OpenMP", category: "Data sharing", name: "lastprivate(var)",
    signature: "#pragma omp for lastprivate(x)",
    summary: "Private copies; the value from the last loop iteration is copied back out.",
    visual: { archetype: "dataSharing", params: { clause: "lastprivate" } }, related: ["omp_firstprivate"],
  },
  {
    id: "omp_reduction", tech: "OpenMP", category: "Data sharing", name: "reduction(op:var)",
    signature: "#pragma omp parallel for reduction(+:sum)",
    summary: "Each thread keeps a private partial; OpenMP combines them with op at the end.",
    note: "The safe, fast way to accumulate (sum, max, &&, ...) — no race, scales well.",
    visual: { archetype: "reduction", params: { threads: 4 } }, exampleId: "reduction",
    related: ["omp_critical", "omp_atomic"],
  },
  {
    id: "omp_default", tech: "OpenMP", category: "Data sharing", name: "default(none)",
    signature: "#pragma omp parallel default(none) shared(...) private(...)",
    summary: "Forces you to declare the sharing of every variable — catches mistakes early.",
    note: "A great habit: with default(none) the compiler errors on anything you forgot to classify.",
    visual: { archetype: "dataSharing", params: { clause: "shared" } }, related: ["omp_shared", "omp_private"],
  },
  {
    id: "omp_task", tech: "OpenMP", category: "Tasking", name: "#pragma omp task",
    signature: "#pragma omp task\n{ /* unit of deferred work */ }",
    summary: "Creates a task that any thread can run later — great for irregular/recursive work.",
    note: "Usually created inside a single region; combine with taskwait/depend to order them.",
    visual: { archetype: "tasks" }, exampleId: "tasks", related: ["omp_taskloop", "omp_taskwait"],
  },
  {
    id: "omp_taskloop", tech: "OpenMP", category: "Tasking", name: "#pragma omp taskloop",
    signature: "#pragma omp taskloop\nfor (...) work(i);",
    summary: "Turns a loop into a set of tasks the team picks up (work-stealing balance).",
    visual: { archetype: "tasks" }, exampleId: "tasks", related: ["omp_task", "omp_schedule_dynamic"],
  },
  {
    id: "omp_depend", tech: "OpenMP", category: "Tasking", name: "depend(in/out:var)",
    signature: "#pragma omp task depend(out:a)\n#pragma omp task depend(in:a)",
    summary: "Declares data dependencies so the runtime orders tasks correctly.",
    visual: { archetype: "tasks" }, related: ["omp_task"],
  },

  {
    id: "omp_num_threads_clause", tech: "OpenMP", category: "Parallel regions", name: "num_threads(n)",
    signature: "#pragma omp parallel num_threads(4)",
    summary: "Sets the team size for just this parallel region.",
    visual: { archetype: "forkJoin", params: { threads: 4 } }, related: ["omp_set_num_threads", "omp_parallel"],
  },
  {
    id: "omp_if_clause", tech: "OpenMP", category: "Parallel regions", name: "if(condition)",
    signature: "#pragma omp parallel if(n > 1000)",
    summary: "Only goes parallel when the condition holds — otherwise runs serially (no fork).",
    note: "Avoids paying thread-creation overhead on small inputs.",
    visual: { archetype: "forkJoin", params: { threads: 4 } }, related: ["omp_parallel"],
  },
  {
    id: "omp_get_max_threads", tech: "OpenMP", category: "Parallel regions", name: "omp_get_max_threads()",
    signature: "int n = omp_get_max_threads();",
    summary: "How many threads the next parallel region would use — valid even in serial code.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["omp_get_num_threads"],
  },
  {
    id: "omp_get_wtime", tech: "OpenMP", category: "Runtime", name: "omp_get_wtime()",
    signature: "double t0 = omp_get_wtime(); /* ... */ double s = omp_get_wtime() - t0;",
    summary: "High-resolution wall-clock seconds — wrap a region to time it.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["mpi_wtime"],
  },
  {
    id: "omp_parallel_sections", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp parallel sections",
    signature: "#pragma omp parallel sections\n{ #pragma omp section ...\n  #pragma omp section ... }",
    summary: "Shorthand for a parallel region plus sections — run independent blocks concurrently.",
    visual: { archetype: "worksharing", params: { kind: "sections" } }, related: ["omp_sections"],
  },
  {
    id: "omp_simd", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp simd",
    signature: "#pragma omp simd\nfor (i = 0; i < N; i++) a[i] = b[i] + c[i];",
    summary: "Vectorizes the loop — one core does several iterations at once with SIMD lanes.",
    note: "Different axis from threads: combine as `parallel for simd` to use both.",
    visual: { archetype: "worksharing", params: { kind: "for", threads: 4 } }, exampleId: "simd", related: ["omp_parallel_for", "acc_vector"],
  },
  {
    id: "omp_schedule_runtime", tech: "OpenMP", category: "Scheduling", name: "schedule(runtime)",
    signature: "#pragma omp for schedule(runtime)   // set OMP_SCHEDULE at run time",
    summary: "Defers the schedule choice to the OMP_SCHEDULE environment variable — tune without recompiling.",
    visual: { archetype: "schedule", params: { kind: "dynamic" } }, related: ["omp_schedule_static", "omp_schedule_dynamic"],
  },
  {
    id: "omp_ordered", tech: "OpenMP", category: "Synchronization", name: "#pragma omp ordered",
    signature: "#pragma omp for ordered\nfor (...) { #pragma omp ordered\n  printf(...); }",
    summary: "Forces part of a parallel loop to run in iteration order (e.g. ordered output).",
    note: "Serializes that part — use sparingly.",
    visual: { archetype: "criticalAtomic", params: { kind: "critical" } }, related: ["omp_critical"],
  },
  {
    id: "omp_flush", tech: "OpenMP", category: "Synchronization", name: "#pragma omp flush",
    signature: "#pragma omp flush(var)",
    summary: "Makes a thread's view of memory consistent with others at that point.",
    note: "Rarely needed by hand — barriers/atomics flush for you.",
    visual: { archetype: "barrier", params: { kind: "barrier" } }, related: ["omp_barrier"],
  },
  {
    id: "omp_threadprivate", tech: "OpenMP", category: "Data sharing", name: "threadprivate(var)",
    signature: "static int counter;\n#pragma omp threadprivate(counter)",
    summary: "Gives each thread its own persistent copy of a global/static variable across regions.",
    visual: { archetype: "dataSharing", params: { clause: "private" } }, related: ["omp_private"],
  },
  {
    id: "omp_target", tech: "OpenMP", category: "Offloading (target)", name: "#pragma omp target",
    signature: "#pragma omp target\n{ /* runs on the GPU/accelerator */ }",
    summary: "Offloads the region to an accelerator (GPU) — OpenMP's equivalent of OpenACC's parallel.",
    note: "Pair with map(...) to move data and teams/distribute to spread work.",
    visual: { archetype: "offload", params: { kind: "parallel" } }, related: ["omp_map", "omp_teams", "acc_parallel"],
  },
  {
    id: "omp_target_data", tech: "OpenMP", category: "Offloading (target)", name: "#pragma omp target data",
    signature: "#pragma omp target data map(tofrom: a[0:N])\n{ /* several target regions reuse a */ }",
    summary: "Keeps data resident on the device across multiple target regions (like acc data).",
    visual: { archetype: "accData", params: { clause: "copy" } }, related: ["omp_map", "acc_data"],
  },
  {
    id: "omp_map", tech: "OpenMP", category: "Offloading (target)", name: "map(to/from/tofrom: a)",
    signature: "#pragma omp target map(to: a[0:N]) map(from: r[0:N])",
    summary: "Controls host↔device data movement: to (in), from (out), tofrom (both), alloc (no copy).",
    visual: { archetype: "accData", params: { clause: "copy" } }, related: ["omp_target_data", "acc_copyin"],
  },
  {
    id: "omp_teams", tech: "OpenMP", category: "Offloading (target)", name: "#pragma omp teams",
    signature: "#pragma omp target teams\n{ /* a league of teams on the device */ }",
    summary: "Creates a league of thread teams on the accelerator (≈ OpenACC gangs / CUDA blocks).",
    visual: { archetype: "offload", params: { kind: "loop" } }, related: ["omp_distribute", "acc_gang"],
  },
  {
    id: "omp_distribute", tech: "OpenMP", category: "Offloading (target)", name: "#pragma omp distribute",
    signature: "#pragma omp target teams distribute parallel for\nfor (...) work(i);",
    summary: "The full offload combo: spread loop iterations across teams, then threads, on the device.",
    visual: { archetype: "offload", params: { kind: "loop" } }, related: ["omp_teams", "acc_parallel_loop"],
  },

  {
    id: "omp_atomic_capture", tech: "OpenMP", category: "Synchronization", name: "atomic capture / read / write / update",
    signature: "#pragma omp atomic capture\n{ old = v; v += x; }",
    summary: "Atomic variants: read, write, update (default), or capture (atomically update AND fetch).",
    note: "capture is how you grab a unique index/old value without a lock.",
    visual: { archetype: "criticalAtomic", params: { kind: "atomic" } }, related: ["omp_atomic", "omp_critical"],
  },
  {
    id: "omp_cancel", tech: "OpenMP", category: "Synchronization", name: "#pragma omp cancel",
    signature: "#pragma omp cancel for   // + #pragma omp cancellation point",
    summary: "Requests early termination of a parallel loop/region (e.g. once a search hits).",
    note: "Needs OMP_CANCELLATION=true; threads stop at cancellation points.",
    visual: { archetype: "barrier", params: { kind: "barrier" } }, related: ["omp_barrier"],
  },
  {
    id: "omp_loop", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp loop",
    signature: "#pragma omp parallel loop\nfor (...) work(i);",
    summary: "A newer, descriptive loop construct: you assert it's parallelizable, the runtime maps it.",
    visual: { archetype: "worksharing", params: { kind: "for", threads: 4 } }, related: ["omp_parallel_for"],
  },
  {
    id: "omp_masked", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp masked",
    signature: "#pragma omp masked [filter(0)]",
    summary: "Like master but generalized — only the thread(s) matching the filter run the block (5.1+).",
    visual: { archetype: "worksharing", params: { kind: "master" } }, related: ["omp_master", "omp_single"],
  },
  {
    id: "omp_proc_bind", tech: "OpenMP", category: "Parallel regions", name: "proc_bind / OMP_PROC_BIND",
    signature: "#pragma omp parallel proc_bind(close)   // or spread / master",
    summary: "Controls thread affinity — whether threads pack together (close) or spread across the machine.",
    note: "Affinity matters a lot for NUMA and cache locality; pair with OMP_PLACES.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["omp_places", "omp_parallel"],
  },
  {
    id: "omp_places", tech: "OpenMP", category: "Parallel regions", name: "OMP_PLACES",
    signature: "export OMP_PLACES=cores   # or threads / sockets / explicit list",
    summary: "Defines the hardware slots threads can be pinned to (cores, hardware threads, sockets).",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["omp_proc_bind"],
  },
  {
    id: "omp_linear", tech: "OpenMP", category: "Data sharing", name: "linear(var:step)",
    signature: "#pragma omp simd linear(j:1)",
    summary: "A variable that increases by a fixed step each iteration — private but kept consistent.",
    visual: { archetype: "dataSharing", params: { clause: "private" } }, related: ["omp_private", "omp_simd"],
  },
  {
    id: "omp_scan", tech: "OpenMP", category: "Data sharing", name: "#pragma omp scan",
    signature: "#pragma omp simd reduction(inscan,+:sum)\nfor (...) { ...; #pragma omp scan inclusive(sum); }",
    summary: "Computes a prefix sum (running total) in parallel — the scan counterpart of reduction.",
    visual: { archetype: "reduction", params: { threads: 4 } }, related: ["omp_reduction", "mpi_scan"],
  },
  {
    id: "omp_declare_reduction", tech: "OpenMP", category: "Data sharing", name: "#pragma omp declare reduction",
    signature: "#pragma omp declare reduction(merge : MyT : omp_out = combine(omp_out, omp_in))",
    summary: "Defines a custom reduction operator for your own types/operations.",
    visual: { archetype: "reduction", params: { threads: 4 } }, related: ["omp_reduction"],
  },
  {
    id: "omp_taskyield", tech: "OpenMP", category: "Tasking", name: "#pragma omp taskyield",
    signature: "#pragma omp taskyield",
    summary: "Suggests the current task may be suspended so the thread can run another task.",
    visual: { archetype: "tasks" }, related: ["omp_task", "omp_taskwait"],
  },
  {
    id: "omp_nested", tech: "OpenMP", category: "Runtime", name: "OMP_NESTED / omp_get_level",
    signature: "omp_set_max_active_levels(2);  int lvl = omp_get_level();",
    summary: "Enables and inspects nested parallelism — a parallel region inside another.",
    note: "Often better to use one level + tasks; nesting can oversubscribe the cores.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["omp_parallel"],
  },
  {
    id: "omp_get_num_procs", tech: "OpenMP", category: "Runtime", name: "omp_get_num_procs()",
    signature: "int p = omp_get_num_procs();",
    summary: "Returns how many processors are available to the program.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["omp_get_max_threads"],
  },

  {
    id: "omp_parallel_for_simd", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp parallel for simd",
    signature: "#pragma omp parallel for simd\nfor (i = 0; i < N; i++) a[i] = b[i] + c[i];",
    summary: "Combines thread parallelism and SIMD vectorization on one loop — both axes at once.",
    visual: { archetype: "worksharing", params: { kind: "for", threads: 4 } }, exampleId: "simd", related: ["omp_simd", "omp_parallel_for"],
  },
  {
    id: "omp_critical_named", tech: "OpenMP", category: "Synchronization", name: "#pragma omp critical(name)",
    signature: "#pragma omp critical(bins)\n{ histogram[b]++; }",
    summary: "A named critical section — only blocks against other criticals with the SAME name.",
    note: "Naming lets independent critical regions run concurrently instead of all serializing together.",
    visual: { archetype: "criticalAtomic", params: { kind: "critical" } }, related: ["omp_critical", "omp_atomic"],
  },
  {
    id: "omp_dist_schedule", tech: "OpenMP", category: "Scheduling", name: "dist_schedule(static, chunk)",
    signature: "#pragma omp distribute dist_schedule(static, 64)",
    summary: "Controls how iterations are distributed across teams (the team-level analog of schedule).",
    visual: { archetype: "schedule", params: { kind: "static" } }, related: ["omp_distribute", "omp_schedule_static"],
  },
  {
    id: "omp_set_dynamic", tech: "OpenMP", category: "Runtime", name: "omp_set_dynamic / OMP_DYNAMIC",
    signature: "omp_set_dynamic(1);   // allow the runtime to vary team size",
    summary: "Lets the runtime adjust the number of threads per region to match the load/system.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["omp_set_num_threads"],
  },
  {
    id: "omp_target_enter_data", tech: "OpenMP", category: "Offloading (target)", name: "#pragma omp target enter data / exit data",
    signature: "#pragma omp target enter data map(to: a[0:N])\n/* ... */\n#pragma omp target exit data map(from: a[0:N])",
    summary: "Unstructured device-data lifetime: put data on the accelerator now, take it off later.",
    note: "The OpenMP analog of OpenACC enter/exit data — not block-scoped.",
    visual: { archetype: "accData", params: { clause: "copyin" } }, related: ["omp_target_data", "acc_enter_data"],
  },
  {
    id: "omp_target_update", tech: "OpenMP", category: "Offloading (target)", name: "#pragma omp target update",
    signature: "#pragma omp target update to(a[0:N])   // or from(...)",
    summary: "Re-synchronizes the host and device copies of data without ending the data region.",
    visual: { archetype: "accData", params: { clause: "update" } }, related: ["omp_map", "acc_update"],
  },
  {
    id: "omp_requires", tech: "OpenMP", category: "Offloading (target)", name: "#pragma omp requires",
    signature: "#pragma omp requires unified_shared_memory",
    summary: "Declares features the program needs (e.g. unified memory, so host & device share addresses).",
    note: "With unified_shared_memory you can often drop explicit map clauses.",
    visual: { archetype: "offload", params: { kind: "parallel" } }, related: ["omp_target", "omp_map"],
  },
  {
    id: "omp_defaultmap", tech: "OpenMP", category: "Offloading (target)", name: "defaultmap(...)",
    signature: "#pragma omp target defaultmap(tofrom: scalar)",
    summary: "Sets the default map behaviour for variables of a category in a target region.",
    visual: { archetype: "accData", params: { clause: "copy" } }, related: ["omp_map", "acc_default_present"],
  },

  {
    id: "omp_atomic_memorder", tech: "OpenMP", category: "Synchronization", name: "atomic seq_cst / acq_rel / relaxed",
    signature: "#pragma omp atomic update seq_cst\nx += 1;",
    summary: "Memory-ordering clauses on atomic that control how the update is ordered against other memory ops.",
    note: "relaxed is fastest (atomicity only); seq_cst is the safe default for ordering.",
    visual: { archetype: "criticalAtomic", params: { kind: "atomic" } }, related: ["omp_atomic", "omp_flush"],
  },
  {
    id: "omp_scope", tech: "OpenMP", category: "Data sharing", name: "#pragma omp scope",
    signature: "#pragma omp scope reduction(+:sum)\n{ ...; }",
    summary: "A structured block (5.1+) that can carry reductions/privates without being a loop.",
    visual: { archetype: "reduction", params: { threads: 4 } }, related: ["omp_reduction", "omp_single"],
  },
  {
    id: "omp_interop", tech: "OpenMP", category: "Offloading (target)", name: "#pragma omp interop",
    signature: "#pragma omp interop init(targetsync: obj)",
    summary: "Interoperates with native device runtimes/streams (e.g. CUDA) — bridges OpenMP and GPU libraries.",
    visual: { archetype: "offload", params: { kind: "routine" } }, related: ["omp_target", "acc_host_data"],
  },
  {
    id: "omp_metadirective", tech: "OpenMP", category: "Advanced", name: "#pragma omp metadirective",
    signature: "#pragma omp metadirective when(device={arch(nvptx)}: target teams) default(parallel for)",
    summary: "Chooses which directive to apply based on context (architecture, device, …) at compile time.",
    note: "Write one loop that compiles to CPU or GPU directives depending on the target.",
    visual: { archetype: "worksharing", params: { kind: "for", threads: 4 } }, related: ["omp_declare_variant", "omp_target"],
  },
  {
    id: "omp_declare_variant", tech: "OpenMP", category: "Advanced", name: "#pragma omp declare variant",
    signature: "#pragma omp declare variant(fast_f) match(construct={target})\ndouble f(double x);",
    summary: "Provides a specialized version of a function the compiler swaps in for matching contexts.",
    visual: { archetype: "worksharing", params: { kind: "for", threads: 4 } }, related: ["omp_metadirective", "omp_routine"],
  },
  {
    id: "omp_get_wtick", tech: "OpenMP", category: "Runtime", name: "omp_get_wtick()",
    signature: "double tick = omp_get_wtick();",
    summary: "The timer resolution (seconds per tick) of omp_get_wtime.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["omp_get_wtime"],
  },
  {
    id: "omp_stacksize", tech: "OpenMP", category: "Runtime", name: "OMP_STACKSIZE",
    signature: "export OMP_STACKSIZE=16M",
    summary: "Sets the per-thread stack size — bump it if deep recursion or big locals overflow.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["omp_wait_policy"],
  },
  {
    id: "omp_wait_policy", tech: "OpenMP", category: "Runtime", name: "OMP_WAIT_POLICY",
    signature: "export OMP_WAIT_POLICY=active   # or passive",
    summary: "Whether idle threads spin (active: low latency, burns a core) or sleep (passive: yields).",
    note: "active helps tight compute; passive is friendlier when oversubscribing or sharing the node.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["omp_proc_bind"],
  },
  {
    id: "omp_taskgroup", tech: "OpenMP", category: "Tasking", name: "#pragma omp taskgroup",
    signature: "#pragma omp taskgroup\n{\n  #pragma omp task\n  do_chunk();   // spawn child tasks\n}   // implicit wait: ALL of them, descendants included",
    signatureF: "!$omp taskgroup\n  !$omp task\n  call do_chunk()   ! spawn child tasks\n  !$omp end task\n!$omp end taskgroup",
    summary: "Waits for every task created in the region — including tasks those tasks spawned — before continuing.",
    note: "Stronger than taskwait, which only waits for the current task's direct children, not grandchildren.",
    visual: { archetype: "tasks", params: { threads: 3 } }, exampleId: "tasks", related: ["omp_task", "omp_taskwait"],
  },
  {
    id: "omp_task_clauses", tech: "OpenMP", category: "Tasking", name: "task clauses (priority / untied / final / if)",
    signature: "#pragma omp task priority(10) untied final(n < 64) if(n > 1000)",
    summary: "Tune a task: priority hints scheduling order, untied lets it resume on any thread, final/if cut off cheap tasks.",
    note: "if(false) / final make a task run immediately and undeferred — the usual fix for too-fine-grained tasking overhead.",
    visual: { archetype: "tasks", params: { threads: 3 } }, related: ["omp_task", "omp_taskloop"],
  },
  {
    id: "omp_declare_simd", tech: "OpenMP", category: "Work-sharing", name: "#pragma omp declare simd",
    signature: "#pragma omp declare simd\nfloat f(float x) { return x * x + 1; }",
    signatureF: "real function f(x)\n  !$omp declare simd(f)\n  real :: x\n  f = x*x + 1\nend function",
    summary: "Tells the compiler to also emit a vectorized version of a function, so it can be called from inside a SIMD loop.",
    note: "Without it, a function call inside `omp simd` blocks vectorization. Add clauses like uniform/linear to describe arguments.",
    visual: { archetype: "worksharing", params: { kind: "for", threads: 4 } }, exampleId: "simd", related: ["omp_simd", "omp_parallel_for_simd"],
  },
  {
    id: "omp_declare_target", tech: "OpenMP", category: "Offloading (target)", name: "#pragma omp declare target",
    signature: "#pragma omp declare target\nint helper(int i);   // compiled for the device too\n#pragma omp end declare target",
    signatureF: "!$omp declare target\ninteger function helper(i)   ! compiled for the device too\n!$omp end declare target",
    summary: "Marks functions and globals to be compiled for the accelerator so device code can call/use them.",
    note: "The OpenMP counterpart of `acc routine` — without it, calling a host function from a target region fails to link.",
    visual: { archetype: "offload", params: { kind: "routine" } }, related: ["omp_target", "acc_routine"],
  },
  {
    id: "omp_in_parallel", tech: "OpenMP", category: "Runtime", name: "omp_in_parallel()",
    signature: "if (omp_in_parallel()) { /* already inside a parallel region */ }",
    signatureF: "if (omp_in_parallel()) then\n  ! already inside a parallel region\nend if",
    summary: "Returns nonzero if the call is made from inside an active parallel region (a team of >1 thread).",
    note: "Handy in library code that must behave differently when already parallelised by the caller.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["omp_get_num_threads", "omp_get_level"],
  },
  {
    id: "omp_get_num_devices", tech: "OpenMP", category: "Offloading (target)", name: "omp_get_num_devices() / default device",
    signature: "int nd = omp_get_num_devices();\nomp_set_default_device(0);\nif (omp_is_initial_device()) { /* running on the host */ }",
    signatureF: "integer :: nd\nnd = omp_get_num_devices()\ncall omp_set_default_device(0)\nif (omp_is_initial_device()) then\n  ! running on the host\nend if",
    summary: "Query how many accelerators are visible, pick the default one, and test whether code is executing on the host vs a device.",
    note: "omp_is_initial_device() is the standard way for code inside a target region to tell host fallback from real offload.",
    visual: { archetype: "offload", params: { kind: "parallel" } }, related: ["omp_target", "omp_requires"],
  },
  {
    id: "omp_get_num_teams", tech: "OpenMP", category: "Offloading (target)", name: "omp_get_num_teams() / omp_get_team_num()",
    signature: "int nt = omp_get_num_teams();\nint tm = omp_get_team_num();",
    signatureF: "integer :: nt, tm\nnt = omp_get_num_teams()\ntm = omp_get_team_num()",
    summary: "Inside a teams region, return the number of teams in the league and the calling team's id.",
    note: "The teams-level analogue of omp_get_num_threads/omp_get_thread_num (≈ CUDA gridDim/blockIdx).",
    visual: { archetype: "offload", params: { kind: "loop" } }, related: ["omp_teams", "omp_distribute"],
  },
  {
    id: "omp_set_schedule", tech: "OpenMP", category: "Scheduling", name: "omp_set_schedule() / omp_get_schedule()",
    signature: "omp_set_schedule(omp_sched_dynamic, 4);   // kind, chunk\n#pragma omp for schedule(runtime)",
    signatureF: "call omp_set_schedule(omp_sched_dynamic, 4)   ! kind, chunk\n!$omp do schedule(runtime)",
    summary: "Sets (or reads) the schedule used by loops compiled with schedule(runtime) — programmatic control without recompiling.",
    note: "The API equivalent of the OMP_SCHEDULE env var; both only affect schedule(runtime) loops.",
    visual: { archetype: "schedule", params: { kind: "dynamic" } }, related: ["omp_schedule_runtime", "omp_schedule_dynamic"],
  },
  {
    id: "omp_target_exit_data", tech: "OpenMP", category: "Offloading (target)", name: "#pragma omp target exit data",
    signature: "#pragma omp target exit data map(from: a[0:N]) map(delete: tmp[0:N])",
    signatureF: "!$omp target exit data map(from: a(1:N)) map(delete: tmp(1:N))",
    summary: "Ends an unstructured device-data region: copy results back (from) and/or free device buffers (delete).",
    note: "Pairs with `target enter data`. Together they manage device memory across many kernels without one enclosing data region.",
    visual: { archetype: "accData", params: { clause: "copyout" } }, related: ["omp_target_enter_data", "omp_target_update"],
  },
  {
    id: "omp_allocators", tech: "OpenMP", category: "Data sharing", name: "#pragma omp allocate / omp_alloc",
    signature: "#pragma omp allocate(buf) allocator(omp_high_bw_mem_alloc)\nstatic double buf[N];\n// runtime form:\ndouble *p = omp_alloc(n * sizeof(double), omp_large_cap_mem_alloc);",
    signatureF: "!$omp allocate(buf) allocator(omp_high_bw_mem_alloc)\nreal(8) :: buf(N)\n! runtime form (iso_c_binding):\n! type(c_ptr) :: p\n! p = omp_alloc(n*8_c_size_t, omp_large_cap_mem_alloc)",
    summary: "Places allocations in a chosen memory space (high-bandwidth, large-capacity, constant, …) via OpenMP allocators.",
    note: "Matters on systems with HBM + DDR (or CXL): steer hot arrays to fast memory without vendor-specific APIs.",
    visual: { archetype: "dataSharing", params: { clause: "private" } }, related: ["omp_private", "omp_requires"],
  },
  {
    id: "omp_target_memory", tech: "OpenMP", category: "Offloading (target)", name: "omp_target_alloc / memcpy / free",
    signature: "void *dp = omp_target_alloc(bytes, dev);\nomp_target_memcpy(dp, hp, bytes, 0, 0, dev, host);\nomp_target_free(dp, dev);",
    signatureF: "type(c_ptr) :: dp\ndp = omp_target_alloc(bytes, dev)\nistat = omp_target_memcpy(dp, hp, bytes, 0_c_size_t, 0_c_size_t, dev, host)\ncall omp_target_free(dp, dev)",
    summary: "Explicit device memory management — allocate, copy, and free device buffers by hand instead of using map clauses.",
    note: "The escape hatch when map(...) isn't flexible enough; hand the device pointer to a target region with is_device_ptr.",
    visual: { archetype: "accData", params: { clause: "create" } }, related: ["omp_map", "omp_target_data", "omp_get_num_devices"],
  },
  {
    id: "omp_order_concurrent", tech: "OpenMP", category: "Work-sharing", name: "order(concurrent)",
    signature: "#pragma omp for order(concurrent)\nfor (i = 0; i < N; i++) a[i] = 2 * b[i];",
    summary: "Asserts the loop's iterations may run in any order, freeing the compiler to vectorize and parallelize aggressively.",
    note: "Your promise that there are no cross-iteration dependencies — unlocks SIMD + threading the compiler wouldn't risk otherwise.",
    visual: { archetype: "worksharing", params: { kind: "for", threads: 4 } }, exampleId: "simd", related: ["omp_simd", "omp_loop"],
  },
  {
    id: "omp_depobj", tech: "OpenMP", category: "Tasking", name: "#pragma omp depobj",
    signature: "#pragma omp depobj(obj) depend(out: a)\n#pragma omp task depend(depobj: obj)",
    summary: "Creates a reusable dependence object so many tasks can share one dependency without re-listing the variables each time.",
    note: "Handy when the same complex dependency recurs across many task creations — declare it once, reference it by handle.",
    visual: { archetype: "tasks", params: { threads: 3 } }, related: ["omp_depend", "omp_task"],
  },
  {
    id: "env_omp_target_offload", tech: "OpenMP", category: "Offloading (target)", name: "OMP_TARGET_OFFLOAD",
    signature: "export OMP_TARGET_OFFLOAD=MANDATORY   # or DISABLED / DEFAULT",
    summary: "Sets offload policy: MANDATORY (fail if no device), DISABLED (force host), DEFAULT (offload when possible).",
    note: "Set MANDATORY in CI to catch silent host-fallback that would otherwise hide a broken GPU build.",
    visual: { archetype: "offload", params: { kind: "parallel" } }, related: ["omp_target", "omp_get_num_devices"],
  },
  {
    id: "env_omp_display_env", tech: "OpenMP", category: "Runtime", name: "OMP_DISPLAY_ENV",
    signature: "export OMP_DISPLAY_ENV=true   # or VERBOSE",
    summary: "Prints the OpenMP version and every internal control variable (threads, schedule, binding, …) at program start.",
    note: "The fastest way to see what OpenMP actually thinks its settings are — invaluable when behaviour surprises you.",
    visual: { archetype: "forkJoin", params: { threads: 6 } }, related: ["env_omp_num_threads", "omp_proc_bind"],
  },

  /* ===================== MPI ===================== */
  {
    id: "mpi_init", tech: "MPI", category: "Setup", name: "MPI_Init / MPI_Finalize",
    signature: "MPI_Init(&argc, &argv);\n/* ... */\nMPI_Finalize();",
    summary: "Start and stop the MPI runtime; bracket every MPI program with these.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_comm_rank", "mpi_comm_size"],
  },
  {
    id: "mpi_comm_rank", tech: "MPI", category: "Setup", name: "MPI_Comm_rank",
    signature: "int rank; MPI_Comm_rank(MPI_COMM_WORLD, &rank);",
    summary: "Gives this process its id (0 .. size-1) within a communicator.",
    note: "Branch on rank to make different processes do different things.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_comm_size", "mpi_init"],
  },
  {
    id: "mpi_comm_size", tech: "MPI", category: "Setup", name: "MPI_Comm_size",
    signature: "int size; MPI_Comm_size(MPI_COMM_WORLD, &size);",
    summary: "Returns how many processes (ranks) are in the communicator.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_comm_rank"],
  },
  {
    id: "mpi_comm_split", tech: "MPI", category: "Setup", name: "MPI_Comm_split",
    signature: "MPI_Comm_split(MPI_COMM_WORLD, color, key, &newcomm);",
    summary: "Partitions ranks into sub-communicators by `color` — independent groups.",
    visual: { archetype: "ranksMemory", params: { threads: 6, kind: "split" } }, related: ["mpi_comm_rank"],
  },
  {
    id: "mpi_wtime", tech: "MPI", category: "Setup", name: "MPI_Wtime",
    signature: "double t = MPI_Wtime();",
    summary: "High-resolution wall-clock seconds — wrap a region to time it.",
    note: "Pair with MPI_Reduce(MAX) to get the slowest rank's time (the true wall time).",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_reduce"],
  },
  {
    id: "mpi_send", tech: "MPI", category: "Point-to-point", name: "MPI_Send",
    signature: "MPI_Send(&x, n, MPI_INT, dest, tag, comm);",
    summary: "Sends a message to one specific rank (blocking: returns when the buffer is reusable).",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_recv", "mpi_isend"],
  },
  {
    id: "mpi_recv", tech: "MPI", category: "Point-to-point", name: "MPI_Recv",
    signature: "MPI_Recv(&x, n, MPI_INT, src, tag, comm, &status);",
    summary: "Receives a message from a rank (blocks until the message arrives).",
    note: "Send/Recv tags and types must match, or you'll deadlock or mis-read.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_send", "mpi_sendrecv"],
  },
  {
    id: "mpi_sendrecv", tech: "MPI", category: "Point-to-point", name: "MPI_Sendrecv",
    signature: "MPI_Sendrecv(sbuf,.., dest,.., rbuf,.., src,.., comm, &st);",
    summary: "Send and receive in one call — avoids deadlock in exchanges (e.g. halo swaps).",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_send", "mpi_recv"],
  },
  {
    id: "mpi_isend", tech: "MPI", category: "Point-to-point", name: "MPI_Isend / MPI_Irecv",
    signature: "MPI_Isend(&x, n, .., &req);  /* ... compute ... */\nMPI_Wait(&req, &status);",
    summary: "Non-blocking: post the transfer, keep computing, then Wait — overlaps comm with compute.",
    note: "The key to hiding communication cost. Don't touch the buffer until Wait returns.",
    visual: { archetype: "pointToPoint", params: { mode: "nonblocking" } }, related: ["mpi_wait", "mpi_send"],
  },
  {
    id: "mpi_wait", tech: "MPI", category: "Point-to-point", name: "MPI_Wait / MPI_Waitall",
    signature: "MPI_Wait(&req, &status);  MPI_Waitall(n, reqs, stats);",
    summary: "Blocks until one (or all) non-blocking operations complete.",
    visual: { archetype: "pointToPoint", params: { mode: "nonblocking" } }, related: ["mpi_isend"],
  },
  {
    id: "mpi_bcast", tech: "MPI", category: "Collectives", name: "MPI_Bcast",
    signature: "MPI_Bcast(&buf, n, type, root, comm);",
    summary: "The root sends the same data to every rank in the communicator.",
    visual: { archetype: "collective", params: { op: "bcast" } }, related: ["mpi_scatter", "mpi_reduce"],
  },
  {
    id: "mpi_scatter", tech: "MPI", category: "Collectives", name: "MPI_Scatter",
    signature: "MPI_Scatter(send, n, .., recv, n, .., root, comm);",
    summary: "The root splits an array and sends one chunk to each rank.",
    visual: { archetype: "collective", params: { op: "scatter" } }, related: ["mpi_gather", "mpi_bcast"],
  },
  {
    id: "mpi_gather", tech: "MPI", category: "Collectives", name: "MPI_Gather",
    signature: "MPI_Gather(send, n, .., recv, n, .., root, comm);",
    summary: "Collects one piece from each rank into an array at the root (inverse of scatter).",
    visual: { archetype: "collective", params: { op: "gather" } }, related: ["mpi_scatter", "mpi_allgather"],
  },
  {
    id: "mpi_allgather", tech: "MPI", category: "Collectives", name: "MPI_Allgather",
    signature: "MPI_Allgather(send, n, .., recv, n, .., comm);",
    summary: "Like gather, but every rank ends up with the full collected array.",
    visual: { archetype: "collective", params: { op: "allgather" } }, related: ["mpi_gather"],
  },
  {
    id: "mpi_reduce", tech: "MPI", category: "Collectives", name: "MPI_Reduce",
    signature: "MPI_Reduce(send, recv, n, type, MPI_SUM, root, comm);",
    summary: "Combines values from all ranks with an op (sum, max, ...) into the root.",
    visual: { archetype: "collective", params: { op: "reduce" } }, related: ["mpi_allreduce", "mpi_gather"],
  },
  {
    id: "mpi_allreduce", tech: "MPI", category: "Collectives", name: "MPI_Allreduce",
    signature: "MPI_Allreduce(send, recv, n, type, MPI_SUM, comm);",
    summary: "Reduce, then give the result to every rank — extremely common (e.g. global sums).",
    visual: { archetype: "collective", params: { op: "allreduce" } }, related: ["mpi_reduce"],
  },
  {
    id: "mpi_alltoall", tech: "MPI", category: "Collectives", name: "MPI_Alltoall",
    signature: "MPI_Alltoall(send, n, .., recv, n, .., comm);",
    summary: "Every rank sends a distinct chunk to every other rank — a full transpose of data.",
    note: "The most communication-heavy collective; can dominate at scale (e.g. FFTs).",
    visual: { archetype: "collective", params: { op: "alltoall" } }, related: ["mpi_allgather"],
  },
  {
    id: "mpi_barrier", tech: "MPI", category: "Collectives", name: "MPI_Barrier",
    signature: "MPI_Barrier(comm);",
    summary: "No rank proceeds until all ranks in the communicator have reached the barrier.",
    visual: { archetype: "collective", params: { op: "barrier" } }, related: ["omp_barrier"],
  },

  {
    id: "mpi_probe", tech: "MPI", category: "Point-to-point", name: "MPI_Probe / MPI_Iprobe",
    signature: "MPI_Probe(src, tag, comm, &status);",
    summary: "Checks for an incoming message (and its size) before you receive it.",
    note: "Use with MPI_Get_count to size a buffer for an unknown-length message.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_recv", "mpi_get_count"],
  },
  {
    id: "mpi_get_count", tech: "MPI", category: "Point-to-point", name: "MPI_Get_count",
    signature: "int n; MPI_Get_count(&status, MPI_INT, &n);",
    summary: "Reads how many elements a received (or probed) message actually contained.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_probe", "mpi_recv"],
  },
  {
    id: "mpi_test", tech: "MPI", category: "Point-to-point", name: "MPI_Test",
    signature: "int done; MPI_Test(&req, &done, &status);",
    summary: "Non-blocking check: returns immediately, telling you whether the request finished.",
    note: "Lets you poll and do other work instead of blocking in MPI_Wait.",
    visual: { archetype: "pointToPoint", params: { mode: "nonblocking" } }, related: ["mpi_wait", "mpi_isend"],
  },
  {
    id: "mpi_scatterv", tech: "MPI", category: "Collectives", name: "MPI_Scatterv / MPI_Gatherv",
    signature: "MPI_Scatterv(send, counts, displs, .., recv, n, .., root, comm);",
    summary: "Like scatter/gather but with per-rank counts — for unequal chunk sizes.",
    visual: { archetype: "collective", params: { op: "scatter" } }, related: ["mpi_scatter", "mpi_gather"],
  },
  {
    id: "mpi_scan", tech: "MPI", category: "Collectives", name: "MPI_Scan / MPI_Exscan",
    signature: "MPI_Scan(send, recv, n, type, MPI_SUM, comm);",
    summary: "Prefix reduction: rank k receives the combined result of ranks 0..k.",
    note: "The building block of parallel prefix sums (scans).",
    visual: { archetype: "collective", params: { op: "reduce" } }, related: ["mpi_reduce", "mpi_allreduce"],
  },
  {
    id: "mpi_reduce_scatter", tech: "MPI", category: "Collectives", name: "MPI_Reduce_scatter",
    signature: "MPI_Reduce_scatter(send, recv, counts, type, MPI_SUM, comm);",
    summary: "Reduces element-wise across ranks, then scatters slices of the result to each rank.",
    visual: { archetype: "collective", params: { op: "reduce" } }, related: ["mpi_allreduce", "mpi_scatter"],
  },
  {
    id: "mpi_ibcast", tech: "MPI", category: "Collectives", name: "MPI_Ibcast (non-blocking collectives)",
    signature: "MPI_Ibcast(&buf, n, type, root, comm, &req);  MPI_Wait(&req, ..);",
    summary: "Non-blocking collectives (Ibcast/Ireduce/...): start it, compute, then complete with Wait.",
    note: "Overlap a global operation with local work, just like non-blocking point-to-point.",
    visual: { archetype: "collective", params: { op: "bcast" } }, related: ["mpi_bcast", "mpi_isend"],
  },
  {
    id: "mpi_cart_create", tech: "MPI", category: "Communicators", name: "MPI_Cart_create",
    signature: "MPI_Cart_create(comm, ndims, dims, periods, reorder, &cart);",
    summary: "Builds a Cartesian (grid) topology so ranks have natural neighbours (for stencils/halos).",
    note: "MPI_Cart_shift then gives you the neighbour ranks to exchange with.",
    visual: { archetype: "ranksMemory", params: { threads: 6, kind: "split" } }, related: ["mpi_comm_split", "mpi_sendrecv"],
  },
  {
    id: "mpi_type_vector", tech: "MPI", category: "Communicators", name: "MPI_Type_contiguous / MPI_Type_vector",
    signature: "MPI_Type_vector(count, blocklen, stride, MPI_INT, &t);\nMPI_Type_commit(&t);",
    summary: "Derived datatypes describe non-contiguous data (e.g. a column/halo) so you can send it in one call.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_send", "mpi_sendrecv"],
  },
  {
    id: "mpi_abort", tech: "MPI", category: "Setup", name: "MPI_Abort",
    signature: "MPI_Abort(MPI_COMM_WORLD, errcode);",
    summary: "Forcibly terminates all ranks in the communicator — for unrecoverable errors.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_init"],
  },

  {
    id: "mpi_send_modes", tech: "MPI", category: "Point-to-point", name: "MPI_Ssend / MPI_Bsend / MPI_Rsend",
    signature: "MPI_Ssend(&x, n, type, dest, tag, comm);   // synchronous",
    summary: "Send modes: synchronous (Ssend), buffered (Bsend), ready (Rsend) — control when send returns.",
    note: "Ssend is great for debugging deadlocks: it can't complete until the matching recv is posted.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_send", "mpi_recv"],
  },
  {
    id: "mpi_waitany", tech: "MPI", category: "Point-to-point", name: "MPI_Waitany / MPI_Waitsome",
    signature: "MPI_Waitany(count, reqs, &index, &status);",
    summary: "Waits for any one (or some) of several non-blocking requests — process whichever finishes first.",
    visual: { archetype: "pointToPoint", params: { mode: "nonblocking" } }, related: ["mpi_wait", "mpi_isend"],
  },
  {
    id: "mpi_win_create", tech: "MPI", category: "One-sided (RMA)", name: "MPI_Win_create / MPI_Win_fence",
    signature: "MPI_Win_create(buf, size, .., comm, &win);\nMPI_Win_fence(0, win);  /* sync epoch */",
    summary: "Exposes a memory window other ranks can directly read/write; fences bracket access epochs.",
    note: "One-sided (RMA): the target rank doesn't call a matching receive.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_put", "mpi_accumulate"],
  },
  {
    id: "mpi_put", tech: "MPI", category: "One-sided (RMA)", name: "MPI_Put / MPI_Get",
    signature: "MPI_Put(&x, n, type, targetRank, disp, n, type, win);",
    summary: "Write into (Put) or read from (Get) another rank's window directly — no matching recv on the target.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_win_create", "mpi_accumulate"],
  },
  {
    id: "mpi_accumulate", tech: "MPI", category: "One-sided (RMA)", name: "MPI_Accumulate",
    signature: "MPI_Accumulate(&x, n, type, target, disp, n, type, MPI_SUM, win);",
    summary: "Like Put, but combines into the remote window with an op (sum, max, ...) atomically.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_put", "mpi_reduce"],
  },
  {
    id: "mpi_iallreduce", tech: "MPI", category: "Collectives", name: "MPI_Iallreduce / MPI_Ireduce",
    signature: "MPI_Iallreduce(send, recv, n, type, MPI_SUM, comm, &req);  MPI_Wait(&req, ..);",
    summary: "Non-blocking collectives: start a global reduce, compute meanwhile, then complete with Wait.",
    visual: { archetype: "collective", params: { op: "allreduce" } }, related: ["mpi_allreduce", "mpi_ibcast"],
  },
  {
    id: "mpi_in_place", tech: "MPI", category: "Collectives", name: "MPI_IN_PLACE",
    signature: "MPI_Allreduce(MPI_IN_PLACE, buf, n, type, MPI_SUM, comm);",
    summary: "A sentinel that reuses the receive buffer as the send buffer — avoids a separate copy.",
    visual: { archetype: "collective", params: { op: "allreduce" } }, related: ["mpi_allreduce", "mpi_reduce"],
  },
  {
    id: "mpi_alltoallv", tech: "MPI", category: "Collectives", name: "MPI_Allgatherv / MPI_Alltoallv",
    signature: "MPI_Alltoallv(send, scounts, sdispls, .., recv, rcounts, rdispls, .., comm);",
    summary: "Variable-size variants of allgather/alltoall — per-rank counts and displacements.",
    visual: { archetype: "collective", params: { op: "alltoall" } }, related: ["mpi_alltoall", "mpi_allgather"],
  },
  {
    id: "mpi_op_create", tech: "MPI", category: "Collectives", name: "MPI_Op_create",
    signature: "MPI_Op_create(myFunc, commute, &op);  MPI_Reduce(.., op, ..);",
    summary: "Defines a custom reduction operator for MPI_Reduce/Allreduce beyond the built-ins.",
    visual: { archetype: "collective", params: { op: "reduce" } }, related: ["mpi_reduce", "omp_declare_reduction"],
  },
  {
    id: "mpi_comm_dup", tech: "MPI", category: "Communicators", name: "MPI_Comm_dup",
    signature: "MPI_Comm_dup(MPI_COMM_WORLD, &newcomm);",
    summary: "Clones a communicator — libraries use this to get a private channel that won't clash with yours.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_comm_split"],
  },
  {
    id: "mpi_cart_shift", tech: "MPI", category: "Communicators", name: "MPI_Cart_shift",
    signature: "MPI_Cart_shift(cart, dim, 1, &src, &dest);",
    summary: "On a Cartesian topology, returns the neighbour ranks to exchange halos with along a dimension.",
    visual: { archetype: "ranksMemory", params: { threads: 6, kind: "split" } }, related: ["mpi_cart_create", "mpi_sendrecv"],
  },
  {
    id: "mpi_get_processor_name", tech: "MPI", category: "Setup", name: "MPI_Get_processor_name",
    signature: "char name[MPI_MAX_PROCESSOR_NAME]; int len;\nMPI_Get_processor_name(name, &len);",
    summary: "Returns the node/host name a rank is running on — handy for mapping ranks to machines.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_comm_rank"],
  },

  {
    id: "mpi_neighbor", tech: "MPI", category: "Collectives", name: "MPI_Neighbor_allgather / alltoall",
    signature: "MPI_Neighbor_allgather(send, .., recv, .., cartComm);",
    summary: "Collectives restricted to topology neighbours — exactly the pattern for halo exchanges.",
    note: "On a Cartesian comm, each rank exchanges only with its grid neighbours, in one call.",
    visual: { archetype: "collective", params: { op: "allgather" } }, related: ["mpi_cart_create", "mpi_sendrecv"],
  },
  {
    id: "mpi_pack", tech: "MPI", category: "Point-to-point", name: "MPI_Pack / MPI_Unpack",
    signature: "MPI_Pack(&x, 1, MPI_INT, buf, size, &pos, comm);  /* ... */ MPI_Send(buf, pos, MPI_PACKED, ..);",
    summary: "Manually packs mixed data into one buffer to send together (alternative to derived datatypes).",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_type_vector", "mpi_send"],
  },
  {
    id: "mpi_send_init", tech: "MPI", category: "Point-to-point", name: "MPI_Send_init / MPI_Start (persistent)",
    signature: "MPI_Send_init(buf, n, .., &req);\nfor (it...) { MPI_Start(&req); /* ... */ MPI_Wait(&req, ..); }",
    summary: "Persistent requests: set up a repeated send/recv once, then cheaply re-fire it each iteration.",
    note: "Cuts per-message setup cost in tight communication loops (e.g. time-stepping).",
    visual: { archetype: "pointToPoint", params: { mode: "nonblocking" } }, related: ["mpi_isend", "mpi_wait"],
  },
  {
    id: "mpi_type_struct", tech: "MPI", category: "Communicators", name: "MPI_Type_create_struct",
    signature: "MPI_Type_create_struct(n, blocklens, displs, types, &t);\nMPI_Type_commit(&t);",
    summary: "Builds a datatype describing a C struct (mixed field types) so you can send it in one message.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_type_vector", "mpi_pack"],
  },
  {
    id: "mpi_group", tech: "MPI", category: "Communicators", name: "MPI_Group_incl / MPI_Comm_create",
    signature: "MPI_Comm_group(comm, &g);\nMPI_Group_incl(g, n, ranks, &sub);\nMPI_Comm_create(comm, sub, &newcomm);",
    summary: "Builds a sub-communicator from an explicit list of ranks (a group), for collectives on a subset.",
    visual: { archetype: "ranksMemory", params: { threads: 6, kind: "split" } }, related: ["mpi_comm_split", "mpi_comm_dup"],
  },
  {
    id: "mpi_dims_create", tech: "MPI", category: "Communicators", name: "MPI_Dims_create",
    signature: "int dims[2] = {0,0}; MPI_Dims_create(size, 2, dims);",
    summary: "Picks a balanced grid shape (e.g. 12 ranks → 4×3) to feed MPI_Cart_create.",
    visual: { archetype: "ranksMemory", params: { threads: 6, kind: "split" } }, related: ["mpi_cart_create"],
  },
  {
    id: "mpi_comm_free", tech: "MPI", category: "Setup", name: "MPI_Comm_free / MPI_Type_free",
    signature: "MPI_Comm_free(&comm);  MPI_Type_free(&type);",
    summary: "Releases communicators, datatypes, and other MPI objects you created — avoid leaks.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_comm_dup", "mpi_type_struct"],
  },
  {
    id: "mpi_file_open", tech: "MPI", category: "Parallel I/O", name: "MPI_File_open / MPI_File_set_view",
    signature: "MPI_File_open(comm, \"out.dat\", MPI_MODE_CREATE|MPI_MODE_WRONLY, info, &fh);\nMPI_File_set_view(fh, disp, etype, filetype, \"native\", info);",
    summary: "Opens a shared file collectively; a 'view' gives each rank its own window into the file.",
    note: "MPI-IO: all ranks read/write one file in parallel, each to its own region.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_file_write"],
  },
  {
    id: "mpi_file_write", tech: "MPI", category: "Parallel I/O", name: "MPI_File_write_at_all / read_at_all",
    signature: "MPI_File_write_at_all(fh, offset, buf, n, MPI_INT, &status);",
    summary: "Each rank writes (or reads) its slice of a shared file at a given offset, collectively.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_file_open"],
  },

  {
    id: "mpi_sendrecv_replace", tech: "MPI", category: "Point-to-point", name: "MPI_Sendrecv_replace",
    signature: "MPI_Sendrecv_replace(buf, n, type, dest, stag, src, rtag, comm, &st);",
    summary: "Sends and receives using one buffer — the received data overwrites the sent data in place.",
    note: "Handy for cyclic shifts (rotate data around a ring of ranks).",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_sendrecv", "mpi_cart_shift"],
  },
  {
    id: "mpi_testall", tech: "MPI", category: "Point-to-point", name: "MPI_Testall / MPI_Testany",
    signature: "int done; MPI_Testall(n, reqs, &done, stats);",
    summary: "Non-blocking poll over many requests — returns whether all (or any) have completed yet.",
    visual: { archetype: "pointToPoint", params: { mode: "nonblocking" } }, related: ["mpi_test", "mpi_waitany"],
  },
  {
    id: "mpi_cancel", tech: "MPI", category: "Point-to-point", name: "MPI_Cancel / MPI_Request_free",
    signature: "MPI_Cancel(&req);  MPI_Request_free(&req);",
    summary: "Attempts to cancel a pending non-blocking operation and release its request handle.",
    visual: { archetype: "pointToPoint", params: { mode: "nonblocking" } }, related: ["mpi_isend", "mpi_wait"],
  },
  {
    id: "mpi_reduce_local", tech: "MPI", category: "Collectives", name: "MPI_Reduce_local",
    signature: "MPI_Reduce_local(inbuf, inoutbuf, n, type, MPI_SUM);",
    summary: "Applies a reduction op to two local buffers — no communication; a building block for custom collectives.",
    visual: { archetype: "collective", params: { op: "reduce" } }, related: ["mpi_reduce", "mpi_op_create"],
  },
  {
    id: "mpi_graph", tech: "MPI", category: "Communicators", name: "MPI_Dist_graph_create",
    signature: "MPI_Dist_graph_create_adjacent(comm, indeg, srcs, .., outdeg, dests, .., info, reorder, &g);",
    summary: "A general graph topology — arbitrary neighbour relations (beyond a Cartesian grid).",
    note: "Pairs with the neighborhood collectives for irregular meshes.",
    visual: { archetype: "ranksMemory", params: { threads: 6, kind: "split" } }, related: ["mpi_cart_create", "mpi_neighbor"],
  },
  {
    id: "mpi_type_commit", tech: "MPI", category: "Communicators", name: "MPI_Type_commit / MPI_Type_size",
    signature: "MPI_Type_commit(&t);  int sz; MPI_Type_size(t, &sz);",
    summary: "Finalizes a derived datatype so it can be used in communication; query its byte size.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_type_vector", "mpi_type_struct"],
  },
  {
    id: "mpi_win_shared", tech: "MPI", category: "One-sided (RMA)", name: "MPI_Win_allocate_shared",
    signature: "MPI_Win_allocate_shared(size, disp, info, shmComm, &ptr, &win);",
    summary: "Allocates a window in true shared memory across ranks on the same node — direct load/store, no copies.",
    note: "MPI+shared-memory: ranks on one node can share data like threads while staying separate processes.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_win_create", "mpi_put"],
  },
  {
    id: "mpi_comm_spawn", tech: "MPI", category: "Dynamic processes", name: "MPI_Comm_spawn",
    signature: "MPI_Comm_spawn(\"worker\", argv, count, info, root, comm, &intercomm, errs);",
    summary: "Launches new MPI processes at run time and connects them via an inter-communicator.",
    note: "Most HPC jobs use a fixed process count; spawning suits master/worker or elastic workloads.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_init", "mpi_comm_dup"],
  },
  {
    id: "mpi_info", tech: "MPI", category: "Setup", name: "MPI_Info_create / MPI_Info_set",
    signature: "MPI_Info info; MPI_Info_create(&info);\nMPI_Info_set(info, \"key\", \"value\");",
    summary: "Key/value hints passed to MPI (I/O striping, RMA, spawn, ...) to guide the implementation.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_file_open", "mpi_comm_spawn"],
  },
  {
    id: "mpi_wtick", tech: "MPI", category: "Setup", name: "MPI_Wtick",
    signature: "double res = MPI_Wtick();",
    summary: "The resolution (seconds per tick) of the MPI_Wtime clock.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_wtime"],
  },
  {
    id: "mpi_init_thread", tech: "MPI", category: "Setup", name: "MPI_Init_thread",
    signature: "int provided;\nMPI_Init_thread(&argc, &argv, MPI_THREAD_MULTIPLE, &provided);\nif (provided < MPI_THREAD_MULTIPLE) { /* fall back */ }",
    signatureF: "integer :: provided, ierror\ncall MPI_Init_thread(MPI_THREAD_MULTIPLE, provided, ierror)",
    summary: "Initialises MPI while requesting a thread-safety level — required when mixing MPI with OpenMP threads.",
    note: "Levels: SINGLE < FUNNELED (only main thread calls MPI) < SERIALIZED (one at a time) < MULTIPLE (any thread, any time). Always check `provided`.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_init", "omp_parallel"],
  },
  {
    id: "mpi_comm_split_type", tech: "MPI", category: "Communicators", name: "MPI_Comm_split_type",
    signature: "MPI_Comm_split_type(comm, MPI_COMM_TYPE_SHARED, rank, MPI_INFO_NULL, &nodeComm);",
    summary: "Splits a communicator by hardware locality — MPI_COMM_TYPE_SHARED yields one sub-communicator per shared-memory node.",
    note: "The standard way to find a rank's node-local peers, e.g. to set up MPI_Win_allocate_shared for hybrid on-node work.",
    visual: { archetype: "ranksMemory", params: { threads: 4, kind: "split" } }, related: ["mpi_comm_split", "mpi_win_shared"],
  },
  {
    id: "mpi_type_subarray", tech: "MPI", category: "Communicators", name: "MPI_Type_create_subarray",
    signature: "MPI_Type_create_subarray(ndims, sizes, subsizes, starts, MPI_ORDER_C, MPI_INT, &t);\nMPI_Type_commit(&t);",
    summary: "Builds a datatype for an n-dimensional sub-block of a larger array — the clean way to describe a tile, halo, or file view.",
    note: "Heavily used with MPI-IO: each rank sets a subarray as its file view and writes its tile in one collective call.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_type_vector", "mpi_file_write"],
  },
  {
    id: "mpi_ops", tech: "MPI", category: "Collectives", name: "predefined reduce ops (MPI_SUM, MPI_MAX, …)",
    signature: "MPI_SUM   MPI_PROD   MPI_MAX   MPI_MIN\nMPI_LAND  MPI_LOR    MPI_BAND  MPI_BOR\nMPI_MAXLOC   MPI_MINLOC   (extremum + its rank)",
    signatureF: "MPI_SUM   MPI_PROD   MPI_MAX   MPI_MIN\nMPI_LAND  MPI_LOR    MPI_BAND  MPI_BOR\nMPI_MAXLOC   MPI_MINLOC   ! extremum + its rank\n! (identical names in the Fortran bindings)",
    summary: "The built-in operators passed to MPI_Reduce / Allreduce / Scan — arithmetic, logical, bitwise, and the index-tracking MAXLOC/MINLOC.",
    note: "MAXLOC/MINLOC return the extremum AND which rank held it. Need something else? Build your own with MPI_Op_create.",
    visual: { archetype: "collective", params: { op: "reduce" } }, related: ["mpi_reduce", "mpi_allreduce", "mpi_op_create"],
  },
  {
    id: "mpi_igather", tech: "MPI", category: "Collectives", name: "MPI_Igather / MPI_Iscatter",
    signature: "MPI_Igather(send, n, .., recv, n, .., root, comm, &req);\n/* ... compute, overlapping the gather ... */\nMPI_Wait(&req, &status);",
    summary: "Non-blocking collectives: start the gather/scatter, overlap other work, then Wait — like Isend but for whole-team operations.",
    note: "Every collective has an I-variant (Ibcast, Ireduce, …). The buffers must stay untouched until Wait/Test completes.",
    visual: { archetype: "collective", params: { op: "gather" } }, related: ["mpi_gather", "mpi_ibcast", "mpi_iallreduce"],
  },
  {
    id: "mpi_type_resized", tech: "MPI", category: "Communicators", name: "MPI_Type_create_resized / MPI_Type_get_extent",
    signature: "MPI_Type_get_extent(oldtype, &lb, &extent);\nMPI_Type_create_resized(oldtype, lb, newextent, &t);\nMPI_Type_commit(&t);",
    summary: "Adjusts a datatype's extent (its stride) so consecutive elements pack correctly — the fix for gappy gather/scatter of derived types.",
    note: "Classic use: resize a column or struct type so MPI_Gather lays the received blocks back-to-back instead of leaving holes.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_type_vector", "mpi_type_subarray"],
  },
  {
    id: "mpi_win_lock", tech: "MPI", category: "One-sided (RMA)", name: "MPI_Win_lock / unlock / flush",
    signature: "MPI_Win_lock(MPI_LOCK_SHARED, target, 0, win);\nMPI_Get(&x, 1, MPI_INT, target, 0, 1, MPI_INT, win);\nMPI_Win_unlock(target, win);",
    summary: "Passive-target RMA: read/write another rank's window without that rank participating — it need not call anything.",
    note: "Unlike fence (active target), the target is uninvolved. MPI_Win_flush completes outstanding ops without ending the access epoch.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_win_create", "mpi_put"],
  },
  {
    id: "mpi_error_handling", tech: "MPI", category: "Setup", name: "MPI_Comm_set_errhandler / MPI_Error_string",
    signature: "MPI_Comm_set_errhandler(MPI_COMM_WORLD, MPI_ERRORS_RETURN);\nint err = MPI_Send(buf, n, MPI_INT, dst, 0, comm);\nMPI_Error_string(err, msg, &len);   // human-readable text",
    summary: "By default MPI aborts on error; switch to MPI_ERRORS_RETURN to get error codes back and translate them to readable text.",
    note: "Essential for fault tolerance and debugging — without it a bad call just kills the whole job with a terse message.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } }, related: ["mpi_abort", "mpi_init"],
  },
  {
    id: "mpi_status", tech: "MPI", category: "Point-to-point", name: "MPI_Status fields / MPI_STATUS_IGNORE",
    signature: "MPI_Recv(&x, 1, MPI_INT, MPI_ANY_SOURCE, 0, comm, &st);\nint who = st.MPI_SOURCE, tag = st.MPI_TAG;\n// don't need it? pass MPI_STATUS_IGNORE",
    signatureF: "integer :: st(MPI_STATUS_SIZE)\ncall MPI_Recv(x, 1, MPI_INTEGER, MPI_ANY_SOURCE, 0, comm, st, ierror)\nwho = st(MPI_SOURCE);  tag = st(MPI_TAG)\n! don't need it? pass MPI_STATUS_IGNORE",
    summary: "After a receive/probe, the status reveals who sent it (MPI_SOURCE), its tag (MPI_TAG), and error — or pass MPI_STATUS_IGNORE to skip it.",
    note: "Indispensable with MPI_ANY_SOURCE/ANY_TAG, where you don't know the sender until the message actually arrives.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_recv", "mpi_get_count", "mpi_wildcards"],
  },
  {
    id: "mpi_wildcards", tech: "MPI", category: "Point-to-point", name: "MPI_ANY_SOURCE / MPI_ANY_TAG / MPI_PROC_NULL",
    signature: "MPI_Recv(&x, 1, MPI_INT, MPI_ANY_SOURCE, MPI_ANY_TAG, comm, &st);\n// MPI_PROC_NULL: a no-op rank — send/recv to it does nothing",
    summary: "Receive wildcards (match any sender / any tag) plus MPI_PROC_NULL, a dummy rank that turns boundary send/recv into safe no-ops.",
    note: "MPI_PROC_NULL cleans up halo-exchange edges: no special-casing the grid boundary — just \"send\" to PROC_NULL.",
    visual: { archetype: "pointToPoint", params: { mode: "blocking" } }, related: ["mpi_recv", "mpi_status", "mpi_cart_shift"],
  },
  {
    id: "mpi_cart_coords", tech: "MPI", category: "Communicators", name: "MPI_Cart_coords / MPI_Cart_rank",
    signature: "MPI_Cart_coords(cart, rank, ndims, coords);   // rank -> grid (i,j)\nMPI_Cart_rank(cart, coords, &rank);           // grid (i,j) -> rank",
    summary: "Convert between a rank id and its (i,j,…) position in a Cartesian topology — the two-way map for grid-structured codes.",
    note: "Use the coords to decide which subdomain a rank owns; pair with MPI_Cart_shift for neighbour exchange.",
    visual: { archetype: "ranksMemory", params: { threads: 6, kind: "split" } }, related: ["mpi_cart_create", "mpi_cart_shift"],
  },

  /* ===================== OpenACC ===================== */
  {
    id: "acc_parallel", tech: "OpenACC", category: "Compute", name: "#pragma acc parallel",
    signature: "#pragma acc parallel [clauses]\n{ /* runs on the accelerator */ }",
    summary: "Launches the region on the accelerator (GPU); you control the parallelism explicitly.",
    note: "You decide gang/worker/vector. Compare with `kernels`, where the compiler decides.",
    visual: { archetype: "offload", params: { kind: "parallel" } }, related: ["acc_kernels", "acc_parallel_loop", "acc_loop"],
  },
  {
    id: "acc_kernels", tech: "OpenACC", category: "Compute", name: "#pragma acc kernels",
    signature: "#pragma acc kernels\n{ for (...) ...; for (...) ...; }",
    summary: "Hands a region to the compiler, which finds the parallel loops and offloads them.",
    note: "Easier to start with; `parallel` gives you more control when the compiler is too cautious.",
    visual: { archetype: "offload", params: { kind: "kernels" } }, related: ["acc_parallel"],
  },
  {
    id: "acc_parallel_loop", tech: "OpenACC", category: "Compute", name: "#pragma acc parallel loop",
    signature: "#pragma acc parallel loop\nfor (i = 0; i < N; i++) work(i);",
    summary: "The common combo: launch on the device and distribute the loop across it.",
    visual: { archetype: "offload", params: { kind: "loop" } }, related: ["acc_parallel", "acc_loop", "omp_parallel_for"],
  },
  {
    id: "acc_loop", tech: "OpenACC", category: "Compute", name: "#pragma acc loop",
    signature: "#pragma acc loop [gang|worker|vector] [independent]\nfor (...) ...",
    summary: "Distributes a loop's iterations across the device's parallelism levels.",
    visual: { archetype: "gangs", params: { level: "gang" } }, related: ["acc_gang", "acc_independent"],
  },
  {
    id: "acc_routine", tech: "OpenACC", category: "Compute", name: "#pragma acc routine",
    signature: "#pragma acc routine seq\ndouble f(double x) { ... }",
    summary: "Marks a function so it can be compiled for and called from device code.",
    visual: { archetype: "offload", params: { kind: "routine" } }, related: ["acc_parallel"],
  },
  {
    id: "acc_data", tech: "OpenACC", category: "Data", name: "#pragma acc data",
    signature: "#pragma acc data copy(a[0:N])\n{ /* several kernels reuse a on the device */ }",
    summary: "Opens a data region so arrays stay on the device across multiple kernels.",
    note: "The #1 OpenACC performance fix: hoist data movement out of the compute loops.",
    visual: { archetype: "accData", params: { clause: "copy" } }, related: ["acc_copyin", "acc_copyout", "acc_present"],
  },
  {
    id: "acc_copyin", tech: "OpenACC", category: "Data", name: "copyin(a[0:N])",
    signature: "#pragma acc parallel loop copyin(a[0:N])",
    summary: "Copies data host → device on entry (read-only input that the device needs).",
    visual: { archetype: "accData", params: { clause: "copyin" } }, related: ["acc_copyout", "acc_copy"],
  },
  {
    id: "acc_copyout", tech: "OpenACC", category: "Data", name: "copyout(a[0:N])",
    signature: "#pragma acc parallel loop copyout(a[0:N])",
    summary: "Copies data device → host on exit (results produced on the device).",
    visual: { archetype: "accData", params: { clause: "copyout" } }, related: ["acc_copyin", "acc_copy"],
  },
  {
    id: "acc_copy", tech: "OpenACC", category: "Data", name: "copy(a[0:N])",
    signature: "#pragma acc data copy(a[0:N])",
    summary: "Copies in on entry and out on exit — for data both read and written on the device.",
    visual: { archetype: "accData", params: { clause: "copy" } }, related: ["acc_copyin", "acc_copyout"],
  },
  {
    id: "acc_create", tech: "OpenACC", category: "Data", name: "create(tmp[0:N])",
    signature: "#pragma acc data create(tmp[0:N])",
    summary: "Allocates device memory with no transfer — for scratch/temporary arrays.",
    visual: { archetype: "accData", params: { clause: "create" } }, related: ["acc_copy", "acc_present"],
  },
  {
    id: "acc_present", tech: "OpenACC", category: "Data", name: "present(a[0:N])",
    signature: "#pragma acc parallel loop present(a[0:N])",
    summary: "Asserts the data is already on the device (from an enclosing data region) — no copy.",
    visual: { archetype: "accData", params: { clause: "present" } }, related: ["acc_data", "acc_enter_data"],
  },
  {
    id: "acc_enter_data", tech: "OpenACC", category: "Data", name: "#pragma acc enter data / exit data",
    signature: "#pragma acc enter data copyin(a[0:N])\n/* ... */\n#pragma acc exit data copyout(a[0:N])",
    summary: "Unstructured data lifetime: put data on the device now, take it off later (not block-scoped).",
    visual: { archetype: "accData", params: { clause: "copyin" } }, related: ["acc_data", "acc_update"],
  },
  {
    id: "acc_update", tech: "OpenACC", category: "Data", name: "#pragma acc update",
    signature: "#pragma acc update device(a[0:N])\n#pragma acc update self(a[0:N])   // self = host",
    summary: "Re-synchronizes the host and device copies of data without leaving the data region.",
    visual: { archetype: "accData", params: { clause: "update" } }, related: ["acc_data"],
  },
  {
    id: "acc_gang", tech: "OpenACC", category: "Parallelism levels", name: "gang",
    signature: "#pragma acc loop gang",
    summary: "Coarsest level — independent blocks of work (≈ CUDA thread blocks / MPI-like).",
    visual: { archetype: "gangs", params: { level: "gang" } }, related: ["acc_worker", "acc_vector"],
  },
  {
    id: "acc_worker", tech: "OpenACC", category: "Parallelism levels", name: "worker",
    signature: "#pragma acc loop worker",
    summary: "Middle level — a group of threads within a gang.",
    visual: { archetype: "gangs", params: { level: "worker" } }, related: ["acc_gang", "acc_vector"],
  },
  {
    id: "acc_vector", tech: "OpenACC", category: "Parallelism levels", name: "vector",
    signature: "#pragma acc loop vector",
    summary: "Finest level — SIMD lanes within a worker (≈ CUDA threads in a warp).",
    visual: { archetype: "gangs", params: { level: "vector" } }, related: ["acc_gang", "acc_worker"],
  },
  {
    id: "acc_num_gangs", tech: "OpenACC", category: "Parallelism levels", name: "num_gangs / vector_length",
    signature: "#pragma acc parallel num_gangs(256) vector_length(128)",
    summary: "Tunes how many gangs and how wide the vector lanes are for the launch.",
    visual: { archetype: "gangs", params: { level: "gang" } }, related: ["acc_gang", "acc_vector"],
  },
  {
    id: "acc_reduction", tech: "OpenACC", category: "Compute", name: "reduction(op:var)",
    signature: "#pragma acc parallel loop reduction(+:sum)",
    summary: "Combines a per-iteration value into one result (sum, max, ...) — same idea as OpenMP.",
    visual: { archetype: "reduction", params: { threads: 4 } }, related: ["omp_reduction", "mpi_reduce"],
  },
  {
    id: "acc_async", tech: "OpenACC", category: "Compute", name: "async / #pragma acc wait",
    signature: "#pragma acc parallel loop async(1)\n#pragma acc wait(1)",
    summary: "Launches device work asynchronously so the host keeps going; `wait` synchronizes.",
    note: "Overlap host work or other kernels with the device — the OpenACC analog of MPI non-blocking.",
    visual: { archetype: "offload", params: { kind: "wait" } }, related: ["mpi_isend"],
  },
  {
    id: "acc_collapse", tech: "OpenACC", category: "Compute", name: "collapse(n)",
    signature: "#pragma acc parallel loop collapse(2)\nfor (i..) for (j..) work(i,j);",
    summary: "Merges n nested loops into one big iteration space — more parallelism for the device.",
    visual: { archetype: "gangs", params: { level: "gang" } }, related: ["acc_loop", "omp_collapse"],
  },
  {
    id: "acc_independent", tech: "OpenACC", category: "Compute", name: "independent",
    signature: "#pragma acc loop independent",
    summary: "Tells the compiler the iterations are independent, so it can parallelize freely.",
    visual: { archetype: "gangs", params: { level: "gang" } }, related: ["acc_loop"],
  },
  {
    id: "acc_default_present", tech: "OpenACC", category: "Data", name: "default(present)",
    signature: "#pragma acc parallel loop default(present)",
    summary: "Assumes every array is already on the device — catches missing data clauses as errors.",
    note: "A good discipline once you manage data with explicit data regions.",
    visual: { archetype: "accData", params: { clause: "present" } }, related: ["acc_present", "acc_data"],
  },
  {
    id: "acc_cache", tech: "OpenACC", category: "Data", name: "#pragma acc cache",
    signature: "#pragma acc cache(a[i-1:3])",
    summary: "Hints that a small slice should be kept in fast on-chip memory for the loop.",
    visual: { archetype: "accData", params: { clause: "present" } }, related: ["acc_loop"],
  },
  {
    id: "acc_host_data", tech: "OpenACC", category: "Data", name: "#pragma acc host_data use_device",
    signature: "#pragma acc host_data use_device(a)\n  some_cuda_library(a);",
    summary: "Exposes the device address of data so you can pass it to CUDA/library calls.",
    note: "The bridge between OpenACC-managed data and hand-written GPU libraries.",
    visual: { archetype: "accData", params: { clause: "present" } }, related: ["acc_data"],
  },
  {
    id: "acc_tile", tech: "OpenACC", category: "Parallelism levels", name: "tile(x,y)",
    signature: "#pragma acc loop tile(32,32)\nfor (i..) for (j..) ...",
    summary: "Blocks nested loops into tiles for better locality and parallel mapping.",
    visual: { archetype: "gangs", params: { level: "worker" } }, related: ["acc_collapse", "acc_loop"],
  },
  {
    id: "acc_seq", tech: "OpenACC", category: "Parallelism levels", name: "seq",
    signature: "#pragma acc loop seq",
    summary: "Runs this loop sequentially on the device (no parallel distribution).",
    visual: { archetype: "gangs", params: { level: "gang" } }, related: ["acc_gang"],
  },
  {
    id: "acc_devices", tech: "OpenACC", category: "Compute", name: "acc_get_num_devices / acc_set_device_num",
    signature: "int n = acc_get_num_devices(acc_device_nvidia);\nacc_set_device_num(0, acc_device_nvidia);",
    summary: "Query how many accelerators are available and pick which one to run on.",
    visual: { archetype: "offload", params: { kind: "parallel" } }, related: ["acc_parallel"],
  },
  {
    id: "acc_serial", tech: "OpenACC", category: "Compute", name: "#pragma acc serial",
    signature: "#pragma acc serial\n{\n  /* one gang on the device — for an inherently serial step between kernels */\n}",
    summary: "Offloads a region that must run sequentially on the device (one gang) — no parallelism, but it avoids a round-trip to the host.",
    note: "Use for an inherently serial step sandwiched between parallel kernels, so the data can stay resident on the device.",
    visual: { archetype: "offload", params: { kind: "parallel" } }, related: ["acc_parallel", "acc_kernels"],
  },
  {
    id: "acc_atomic", tech: "OpenACC", category: "Compute", name: "#pragma acc atomic",
    signature: "#pragma acc atomic update\nsum += a[i];",
    summary: "Makes a single memory update atomic on the device, so concurrent gangs/vector lanes don't race on it.",
    note: "Like OpenMP atomic. Prefer a reduction when you're combining values across a whole loop.",
    visual: { archetype: "criticalAtomic", params: { kind: "atomic" } }, related: ["acc_reduction", "omp_atomic"],
  },
  {
    id: "acc_wait", tech: "OpenACC", category: "Compute", name: "#pragma acc wait",
    signature: "#pragma acc wait\n#pragma acc wait(2)",
    summary: "Blocks until asynchronous device work finishes — wait for everything, or just one numbered queue. The join for async kernels/copies.",
    note: "Pair with async(n): launch overlapping work on numbered queues, then wait on them. acc_wait_all() is the runtime-call form.",
    visual: { archetype: "offload", params: { kind: "wait" } }, related: ["acc_async"],
  },
  {
    id: "acc_exit_data", tech: "OpenACC", category: "Data", name: "#pragma acc exit data",
    signature: "#pragma acc exit data copyout(a[0:N]) delete(tmp[0:N])",
    summary: "Ends an unstructured data lifetime: copy results back (copyout) and/or free device buffers (delete).",
    note: "Pairs with `enter data`. Together they manage device memory across functions without one enclosing data region.",
    visual: { archetype: "accData", params: { clause: "copyout" } }, related: ["acc_enter_data", "acc_update"],
  },
  {
    id: "acc_declare", tech: "OpenACC", category: "Data", name: "#pragma acc declare",
    signature: "#pragma acc declare create(g)",
    summary: "Attaches a device data lifetime to a global/module/local variable right at its declaration — no explicit data region needed.",
    note: "Ideal for lookup tables and module globals every kernel uses; the device copy persists for the program (or routine).",
    visual: { archetype: "accData", params: { clause: "create" } }, related: ["acc_data", "acc_create"],
  },
  {
    id: "acc_deviceptr", tech: "OpenACC", category: "Data", name: "deviceptr(ptr)",
    signature: "#pragma acc parallel loop deviceptr(d_a)\nfor (i = 0; i < N; i++) d_a[i] *= 2;",
    summary: "Tells OpenACC a pointer is ALREADY a device address (e.g. from cudaMalloc) — use it directly, with no mapping or copy.",
    note: "The bridge to CUDA and GPU libraries: hand OpenACC kernels memory another tool allocated on the device.",
    visual: { archetype: "accData", params: { clause: "present" } }, related: ["acc_present", "acc_host_data"],
  },
  {
    id: "acc_vector_length", tech: "OpenACC", category: "Parallelism levels", name: "num_workers / vector_length",
    signature: "#pragma acc parallel num_gangs(256) num_workers(4) vector_length(32)",
    summary: "Sizes the three levels by hand: how many gangs, workers per gang, and vector lanes per worker.",
    note: "Usually let the compiler choose; tune these only when profiling shows the default mapping is poor.",
    visual: { archetype: "gangs", params: { level: "vector" } }, related: ["acc_num_gangs", "acc_gang", "acc_vector"],
  },
  {
    id: "acc_init", tech: "OpenACC", category: "Compute", name: "acc_init / acc_shutdown",
    signature: "acc_init(acc_device_nvidia);\n/* ... */\nacc_shutdown(acc_device_nvidia);",
    summary: "Explicitly starts (or tears down) the accelerator runtime, so the one-time device setup cost isn't charged to your first kernel's timing.",
    note: "Call acc_init before you start timing — otherwise the first kernel silently pays the context-creation cost.",
    visual: { archetype: "offload", params: { kind: "parallel" } }, related: ["acc_devices", "env_acc_device"],
  },
  {
    id: "env_acc_device", tech: "OpenACC", category: "Compute", name: "ACC_DEVICE_TYPE / ACC_DEVICE_NUM",
    signature: "export ACC_DEVICE_TYPE=nvidia   # or host, radeon, ...\nexport ACC_DEVICE_NUM=0",
    summary: "Choose the accelerator type and which device to run on at launch — no recompile.",
    note: "Set ACC_DEVICE_TYPE=host to run the very same binary on the CPU for debugging and correctness checks.",
    visual: { archetype: "offload", params: { kind: "parallel" } }, related: ["acc_devices", "acc_init"],
  },

  /* ===================== Slurm ===================== */
  {
    id: "slurm_sbatch", tech: "Slurm", category: "Job submission", name: "sbatch",
    signature: "sbatch [options] script.sh\n# common options:\n#SBATCH --nodes=4\n#SBATCH --ntasks-per-node=24\n#SBATCH --time=01:00:00\n#SBATCH --partition=gpu",
    summary: "Submit a batch job script to the Slurm scheduler — runs when resources become available.",
    note: "Embed resource requests as #SBATCH directives inside the script rather than on the command line — that way the script is self-documenting and reproducible.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } },
    related: ["slurm_squeue", "slurm_scancel", "slurm_srun"],
  },
  {
    id: "slurm_srun", tech: "Slurm", category: "Job submission", name: "srun",
    signature: "srun --nodes=4 --ntasks=96 ./a.out\n# inside a batch script:\nsrun ./a.out",
    summary: "Launch a parallel job step — can be used inside sbatch scripts or interactively to allocate and run.",
    note: "Inside a batch script srun inherits the #SBATCH allocation; on the login node it requests new resources. For MPI, srun replaces mpirun.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } },
    related: ["slurm_sbatch", "slurm_salloc"],
  },
  {
    id: "slurm_salloc", tech: "Slurm", category: "Job submission", name: "salloc",
    signature: "salloc --nodes=2 --ntasks-per-node=24 --time=00:30:00\n# then: srun ./a.out",
    summary: "Request an interactive allocation — gives you a shell on the compute nodes for debugging.",
    note: "Don't do heavy work on the login node — use salloc to get a compute-node shell, then srun your executable.",
    visual: { archetype: "ranksMemory", params: { threads: 2 } },
    related: ["slurm_srun", "slurm_sbatch"],
  },
  {
    id: "slurm_squeue", tech: "Slurm", category: "Monitoring", name: "squeue",
    signature: "squeue -u $USER           # your jobs\nsqueue -p gpu             # all jobs on a partition\nsqueue --job 12345        # one job",
    summary: "List pending and running jobs in the queue with state, nodes, and estimated start time.",
    note: "Job state: PD = pending (waiting for resources), R = running, CG = completing. Use -l for long format.",
    visual: { archetype: "ranksMemory", params: { threads: 3 } },
    related: ["slurm_sbatch", "slurm_scancel"],
  },
  {
    id: "slurm_scancel", tech: "Slurm", category: "Monitoring", name: "scancel",
    signature: "scancel 12345            # cancel one job\nscancel -u $USER         # cancel all your jobs\nscancel --name myjob",
    summary: "Cancel a pending or running job — sends SIGTERM then SIGKILL after a grace period.",
    note: "Use scancel --signal=USR1 <jobid> to send a custom signal (e.g. checkpoint a job before cancelling).",
    visual: { archetype: "ranksMemory", params: { threads: 2 } },
    related: ["slurm_squeue"],
  },
  {
    id: "slurm_sacct", tech: "Slurm", category: "Monitoring", name: "sacct",
    signature: "sacct -j 12345 --format=JobID,State,Elapsed,MaxRSS,CPUTime\nsacct -u $USER --starttime=today",
    summary: "Query the job accounting database — shows completed jobs, CPU-hours, and peak memory.",
    note: "MaxRSS shows peak resident set size per step — useful for checking if your job is close to the memory limit.",
    visual: { archetype: "ranksMemory", params: { threads: 2 } },
    related: ["slurm_squeue", "slurm_seff"],
  },
  {
    id: "slurm_seff", tech: "Slurm", category: "Monitoring", name: "seff",
    signature: "seff 12345",
    summary: "Print a short CPU and memory efficiency summary for a completed job — how much of the allocation you actually used.",
    note: "A CPU efficiency of 50% means half your cores were idle. A memory efficiency of 20% means you over-requested — request less next time to get higher queue priority.",
    visual: { archetype: "ranksMemory", params: { threads: 2 } },
    related: ["slurm_sacct"],
  },
  {
    id: "slurm_nodes", tech: "Slurm", category: "Resource directives", name: "--nodes / --ntasks / --cpus-per-task",
    signature: "#SBATCH --nodes=4\n#SBATCH --ntasks-per-node=1     # MPI ranks per node\n#SBATCH --cpus-per-task=24      # OMP threads per rank",
    summary: "The three axes of Slurm allocation: nodes, MPI tasks (ranks), and CPU threads per task.",
    note: "For hybrid MPI+OpenMP: set ntasks-per-node=1 (one rank/node) and cpus-per-task=24, then export OMP_NUM_THREADS=24 in your script.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } },
    related: ["slurm_sbatch", "slurm_srun"],
  },
  {
    id: "slurm_gpu", tech: "Slurm", category: "Resource directives", name: "--gres=gpu",
    signature: "#SBATCH --partition=gpu\n#SBATCH --gres=gpu:1            # request 1 GPU\n#SBATCH --gres=gpu:a100:2       # request 2 A100s",
    summary: "Request one or more GPUs as a Generic RESource (GRES) — makes CUDA_VISIBLE_DEVICES available automatically.",
    note: "Always check sinfo -p <partition> -o '%G' to see available GPU types before submitting.",
    visual: { archetype: "offload", params: { kind: "parallel" } },
    related: ["slurm_sbatch", "slurm_nodes"],
  },
  {
    id: "slurm_array", tech: "Slurm", category: "Advanced", name: "--array",
    signature: "#SBATCH --array=1-100\n#SBATCH --array=1-100%8   # at most 8 concurrent\n# inside script:\necho \"Task $SLURM_ARRAY_TASK_ID\"",
    summary: "Submit many independent jobs as one array — each gets a unique SLURM_ARRAY_TASK_ID to select its input.",
    note: "Use %N to cap concurrency so you don't saturate the cluster. Ideal for parameter sweeps — one sbatch, zero shell loops.",
    visual: { archetype: "worksharing", params: { threads: 8 } },
    related: ["slurm_sbatch", "slurm_squeue"],
  },
  {
    id: "slurm_env", tech: "Slurm", category: "Advanced", name: "Slurm environment variables",
    signature: "SLURM_JOB_ID         # current job id\nSLURM_JOB_NODELIST   # e.g. node[01-04]\nSLURM_NTASKS         # total MPI ranks\nSLURM_ARRAY_TASK_ID  # array index",
    summary: "Environment variables automatically set by Slurm inside every job — use them to build portable scripts.",
    note: "Use $SLURM_NTASKS instead of hardcoding the rank count so the same script scales when you change --ntasks.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } },
    related: ["slurm_array", "slurm_sbatch"],
  },

  {
    id: "slurm_sinfo", tech: "Slurm", category: "Monitoring", name: "sinfo",
    signature: "sinfo                     # full overview\nsinfo -p gpu -o '%N %G %t %m'   # GPU partition detail\nsinfo -N -l               # per-node view",
    summary: "Show the cluster's partitions, nodes, and their state — the first thing to check before sizing a job.",
    note: "Node states: idle = free, alloc = in use, mix = partially allocated, drain/down = unavailable. Use sinfo to match your --nodes/--ntasks to what's actually available.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } },
    related: ["slurm_sbatch", "slurm_squeue"],
  },
  {
    id: "slurm_dependency", tech: "Slurm", category: "Advanced", name: "--dependency",
    signature: "#SBATCH --dependency=afterok:12345     # run after job 12345 succeeds\n#SBATCH --dependency=afterany:12345    # run regardless of exit code\n#SBATCH --dependency=singleton         # only one job with this name at a time",
    summary: "Chain jobs into a pipeline — this job waits until the specified condition on another job is met.",
    note: "Common pattern: submit a build job, then submit the run with --dependency=afterok:$BUILDID, then the analysis with --dependency=afterok:$RUNID. Use $(sbatch --parsable script.sh) to capture the job ID for chaining.",
    visual: { archetype: "barrier", params: { threads: 3 } },
    related: ["slurm_sbatch", "slurm_array"],
  },
  {
    id: "slurm_mem", tech: "Slurm", category: "Resource directives", name: "--mem / --mem-per-cpu",
    signature: "#SBATCH --mem=64G              # total memory per node\n#SBATCH --mem-per-cpu=4G       # memory per allocated CPU core\n#SBATCH --mem=0                # request all available memory",
    summary: "Request memory for your job — jobs that exceed this limit are killed by the OOM handler.",
    note: "If you don't request enough, the kernel OOM-killer terminates your process mid-run with no checkpoint. If you request too much, your job waits longer in the queue. Check sacct --format=MaxRSS after a test run to right-size.",
    visual: { archetype: "ranksMemory", params: { threads: 2 } },
    related: ["slurm_nodes", "slurm_sacct", "slurm_seff"],
  },
  {
    id: "slurm_time", tech: "Slurm", category: "Resource directives", name: "--time",
    signature: "#SBATCH --time=04:00:00        # 4 hours\n#SBATCH --time=1-12:00:00      # 1 day 12 hours\n#SBATCH --time=30:00           # 30 minutes",
    summary: "Set the maximum wall-clock time — the job is killed when this limit is reached.",
    note: "Shorter time limits often get higher scheduling priority (backfill). Request 2× your expected runtime for safety, but not 10× — the scheduler uses your time limit to plan backfill slots.",
    visual: { archetype: "timeline", params: { threads: 4 } },
    related: ["slurm_sbatch", "slurm_nodes"],
  },
  {
    id: "slurm_output", tech: "Slurm", category: "Job submission", name: "--output / --error",
    signature: "#SBATCH --output=logs/%x_%j.out    # %x=job name, %j=job ID\n#SBATCH --error=logs/%x_%j.err\n#SBATCH --output=/dev/null          # discard stdout",
    summary: "Redirect job stdout and stderr to files — by default both go to slurm-<jobid>.out in the submit directory.",
    note: "Create the output directory before submitting (mkdir -p logs). Use %A for array job ID and %a for array task index.",
    visual: { archetype: "ranksMemory", params: { threads: 2 } },
    related: ["slurm_sbatch", "slurm_array"],
  },
  {
    id: "slurm_exclusive", tech: "Slurm", category: "Resource directives", name: "--exclusive",
    signature: "#SBATCH --exclusive              # whole-node allocation\n#SBATCH --nodes=1 --exclusive    # one full node, no sharing",
    summary: "Request exclusive access to entire nodes — no other jobs share the hardware.",
    note: "Essential for benchmarking: shared nodes introduce cache/memory/network interference from other users' jobs. For production runs with consistent timing, always use --exclusive.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } },
    related: ["slurm_nodes", "slurm_sbatch"],
  },
  {
    id: "slurm_scontrol", tech: "Slurm", category: "Monitoring", name: "scontrol",
    signature: "scontrol show job 12345        # full job details\nscontrol hold 12345            # prevent from starting\nscontrol release 12345         # allow to start\nscontrol show partition gpu",
    summary: "Inspect and control jobs, nodes, and partitions — the admin-level Slurm command.",
    note: "scontrol show job gives you everything: the submit command, resource request, node list, start/end time, exit code — invaluable for debugging failed jobs.",
    visual: { archetype: "ranksMemory", params: { threads: 3 } },
    related: ["slurm_squeue", "slurm_sacct"],
  },
  {
    id: "slurm_constraint", tech: "Slurm", category: "Resource directives", name: "--constraint",
    signature: "#SBATCH --constraint=avx512      # need AVX-512 capable nodes\n#SBATCH --constraint=a100        # need A100 GPUs\n#SBATCH --constraint='[rack1|rack2]'  # locality",
    summary: "Select nodes by feature tags — ensures your job runs on hardware that matches its requirements.",
    note: "Features are defined by the admin (scontrol show node <name>). Common uses: CPU microarchitecture (avx512, epyc), GPU type (a100, h100), network (ib, hdr), or rack locality for communication-heavy MPI jobs.",
    visual: { archetype: "ranksMemory", params: { threads: 2 } },
    related: ["slurm_nodes", "slurm_gpu", "slurm_sinfo"],
  },

  /* ===================== pthreads ===================== */
  {
    id: "pt_create", tech: "pthreads", category: "Thread lifecycle", name: "pthread_create",
    signature: "pthread_t tid;\npthread_create(&tid, NULL, thread_fn, arg);",
    summary: "Spawn a new POSIX thread that starts executing thread_fn(arg) — the lowest-level thread primitive on Linux.",
    note: "Always pthread_join or pthread_detach every thread you create — unjoined threads leak resources.",
    visual: { archetype: "forkJoin", params: { threads: 4 } },
    related: ["pt_join", "pt_detach"],
  },
  {
    id: "pt_join", tech: "pthreads", category: "Thread lifecycle", name: "pthread_join",
    signature: "void *retval;\npthread_join(tid, &retval);   // blocks until thread exits",
    summary: "Wait for a thread to finish and reclaim its resources — the pthread equivalent of a barrier for one thread.",
    note: "A thread is joinable by default. You must call either pthread_join or pthread_detach exactly once per thread.",
    visual: { archetype: "barrier", params: { threads: 4 } },
    related: ["pt_create", "pt_detach"],
  },
  {
    id: "pt_detach", tech: "pthreads", category: "Thread lifecycle", name: "pthread_detach",
    signature: "pthread_detach(tid);   // or at creation:\npthread_attr_setdetachstate(&attr, PTHREAD_CREATE_DETACHED);",
    summary: "Mark a thread as detached — it cleans up automatically on exit with no need for pthread_join.",
    note: "Use for fire-and-forget threads (e.g. background loggers). You can no longer join a detached thread.",
    visual: { archetype: "forkJoin", params: { threads: 4 } },
    related: ["pt_create", "pt_join"],
  },
  {
    id: "pt_mutex", tech: "pthreads", category: "Synchronization", name: "pthread_mutex_t",
    signature: "pthread_mutex_t mtx = PTHREAD_MUTEX_INITIALIZER;\npthread_mutex_lock(&mtx);\n  /* critical section */\npthread_mutex_unlock(&mtx);",
    summary: "A mutual-exclusion lock — only one thread enters the critical section at a time.",
    note: "Prefer PTHREAD_MUTEX_INITIALIZER for static mutexes. For dynamic ones, call pthread_mutex_init and pthread_mutex_destroy.",
    visual: { archetype: "criticalAtomic", params: { kind: "critical" } },
    related: ["pt_trylock", "pt_rwlock", "pt_cond"],
  },
  {
    id: "pt_trylock", tech: "pthreads", category: "Synchronization", name: "pthread_mutex_trylock",
    signature: "if (pthread_mutex_trylock(&mtx) == 0) {\n  /* got the lock */\n  pthread_mutex_unlock(&mtx);\n} else { /* lock busy */ }",
    summary: "Non-blocking mutex acquire — returns immediately with EBUSY if the lock is held.",
    note: "Useful for work-stealing or back-off loops, but beware livelock if both threads always find the lock busy.",
    visual: { archetype: "criticalAtomic", params: { kind: "critical" } },
    related: ["pt_mutex"],
  },
  {
    id: "pt_rwlock", tech: "pthreads", category: "Synchronization", name: "pthread_rwlock_t",
    signature: "pthread_rwlock_t rwl = PTHREAD_RWLOCK_INITIALIZER;\npthread_rwlock_rdlock(&rwl);   // many readers OK\npthread_rwlock_wrlock(&rwl);   // exclusive write\npthread_rwlock_unlock(&rwl);",
    summary: "A reader-writer lock — allows many concurrent readers but only one writer at a time.",
    note: "Use when reads far outnumber writes. Writer starvation is possible on some implementations — check PTHREAD_RWLOCK_PREFER_WRITER_NONRECURSIVE_NP if needed.",
    visual: { archetype: "reduction", params: { threads: 6 } },
    related: ["pt_mutex"],
  },
  {
    id: "pt_cond", tech: "pthreads", category: "Synchronization", name: "pthread_cond_t",
    signature: "pthread_cond_t cv = PTHREAD_COND_INITIALIZER;\n// waiter:\npthread_mutex_lock(&mtx);\nwhile (!ready) pthread_cond_wait(&cv, &mtx);\npthread_mutex_unlock(&mtx);\n// signaller:\npthread_mutex_lock(&mtx); ready = 1;\npthread_cond_signal(&cv);\npthread_mutex_unlock(&mtx);",
    summary: "Condition variable — lets one thread sleep until another signals that a condition has become true.",
    note: "Always check the predicate in a while loop, not if — spurious wakeups are possible on Linux.",
    visual: { archetype: "barrier", params: { threads: 4 } },
    related: ["pt_mutex", "pt_barrier"],
  },
  {
    id: "pt_barrier", tech: "pthreads", category: "Synchronization", name: "pthread_barrier_t",
    signature: "pthread_barrier_t bar;\npthread_barrier_init(&bar, NULL, NUM_THREADS);\n// in each thread:\npthread_barrier_wait(&bar);   // all block until N arrive",
    summary: "A reusable barrier — all threads block at pthread_barrier_wait until the configured count have arrived.",
    note: "One thread receives PTHREAD_BARRIER_SERIAL_THREAD as the return value — you can use that to run a post-barrier reduction in exactly one thread.",
    visual: { archetype: "barrier", params: { threads: 6 } },
    related: ["pt_cond", "pt_mutex"],
  },
  {
    id: "pt_sem", tech: "pthreads", category: "Synchronization", name: "sem_t (POSIX semaphore)",
    signature: "#include <semaphore.h>\nsem_t sem;\nsem_init(&sem, 0, initial_count);\nsem_wait(&sem);   // decrement (block if 0)\nsem_post(&sem);   // increment (wake a waiter)",
    summary: "A counting semaphore — generalizes a mutex; allows up to N concurrent holders.",
    note: "sem_wait is equivalent to P() and sem_post to V() in classic CS notation. For named semaphores (IPC), use sem_open.",
    visual: { archetype: "criticalAtomic", params: { kind: "atomic" } },
    related: ["pt_mutex", "pt_cond"],
  },
  {
    id: "pt_attr", tech: "pthreads", category: "Thread lifecycle", name: "pthread_attr_t",
    signature: "pthread_attr_t attr;\npthread_attr_init(&attr);\npthread_attr_setstacksize(&attr, 4 * 1024 * 1024);  // 4 MB\npthread_attr_setdetachstate(&attr, PTHREAD_CREATE_JOINABLE);\npthread_create(&tid, &attr, fn, arg);\npthread_attr_destroy(&attr);",
    summary: "Thread attribute object — controls stack size, detach state, scheduling policy, and CPU affinity at creation time.",
    note: "Stack overflow crashes silently on many platforms. If your threads use large local arrays, increase the stack size via setstacksize.",
    visual: { archetype: "forkJoin", params: { threads: 4 } },
    related: ["pt_create", "pt_affinity"],
  },
  {
    id: "pt_affinity", tech: "pthreads", category: "Advanced", name: "pthread_setaffinity_np",
    signature: "cpu_set_t cpus;\nCPU_ZERO(&cpus); CPU_SET(core_id, &cpus);\npthread_setaffinity_np(tid, sizeof(cpus), &cpus);",
    summary: "Pin a thread to specific CPU cores — reduces cache migration and NUMA remote accesses.",
    note: "The _np suffix means non-portable (Linux/glibc only). On macOS use thread_policy_set. Combine with numactl for socket-level placement.",
    visual: { archetype: "dataSharing", params: {} },
    related: ["pt_attr", "pt_create"],
  },
  {
    id: "pt_once", tech: "pthreads", category: "Advanced", name: "pthread_once",
    signature: "pthread_once_t flag = PTHREAD_ONCE_INIT;\nvoid init() { /* runs exactly once */ }\npthread_once(&flag, init);  // safe from any thread",
    summary: "Guarantee a function runs exactly once regardless of how many threads call it — thread-safe singleton initialization.",
    note: "Equivalent to C++11 static local initialization. Prefer this over a double-checked lock pattern, which is error-prone without the right memory-model barriers.",
    visual: { archetype: "criticalAtomic", params: { kind: "critical" } },
    related: ["pt_mutex"],
  },
  {
    id: "pt_tls", tech: "pthreads", category: "Advanced", name: "pthread_key_t (TLS)",
    signature: "pthread_key_t key;\npthread_key_create(&key, destructor);\npthread_setspecific(key, ptr);   // per-thread store\nvoid *p = pthread_getspecific(key);",
    summary: "Thread-local storage — each thread gets its own independent copy of the value associated with a key.",
    note: "Prefer __thread or thread_local (C11/C++11) for static TLS, which the compiler optimises better. Use pthread_key only for dynamic TLS or when you need the per-thread destructor.",
    visual: { archetype: "dataSharing", params: {} },
    related: ["pt_create"],
  },

  {
    id: "pt_self", tech: "pthreads", category: "Thread lifecycle", name: "pthread_self / pthread_equal",
    signature: "pthread_t me = pthread_self();\nif (pthread_equal(me, main_tid))\n  printf(\"I am the main thread\\n\");",
    summary: "Get the calling thread's ID and compare thread IDs — the only portable way to check thread identity.",
    note: "pthread_t is opaque — you cannot compare with == on all platforms. Always use pthread_equal. Useful for implementing leader-election patterns without a barrier.",
    visual: { archetype: "forkJoin", params: { threads: 4 } },
    related: ["pt_create"],
  },
  {
    id: "pt_cancel", tech: "pthreads", category: "Thread lifecycle", name: "pthread_cancel",
    signature: "pthread_cancel(tid);   // request cancellation\n// target thread:\npthread_setcancelstate(PTHREAD_CANCEL_ENABLE, NULL);\npthread_setcanceltype(PTHREAD_CANCEL_DEFERRED, NULL);\npthread_testcancel();  // explicit cancellation point",
    summary: "Request that a thread terminate — the thread is cancelled at its next cancellation point (e.g. pthread_testcancel, read, write, sleep).",
    note: "Deferred cancellation (the default) only acts at cancellation points — this is safer than asynchronous cancellation, which can leave mutexes locked. Pair with pthread_cleanup_push for resource cleanup.",
    visual: { archetype: "forkJoin", params: { threads: 4 } },
    related: ["pt_create", "pt_join", "pt_cleanup"],
  },
  {
    id: "pt_cleanup", tech: "pthreads", category: "Thread lifecycle", name: "pthread_cleanup_push / pop",
    signature: "pthread_cleanup_push(cleanup_fn, resource);\n  /* work that might be cancelled */\n  pthread_mutex_lock(&mtx);\n  /* ... */\n  pthread_mutex_unlock(&mtx);\npthread_cleanup_pop(0);   // 0 = don't execute, just remove",
    summary: "Register a cleanup handler that runs if the thread is cancelled or calls pthread_exit — the pthreads equivalent of a destructor or finally block.",
    note: "Push and pop must be in the same lexical scope (they expand to { and }). Always push a mutex-unlock handler before a cancellation point inside a locked region.",
    visual: { archetype: "barrier", params: { threads: 3 } },
    related: ["pt_cancel", "pt_mutex"],
  },
  {
    id: "pt_spinlock", tech: "pthreads", category: "Synchronization", name: "pthread_spinlock_t",
    signature: "pthread_spinlock_t spl;\npthread_spin_init(&spl, PTHREAD_PROCESS_PRIVATE);\npthread_spin_lock(&spl);\n  /* very short critical section */\npthread_spin_unlock(&spl);\npthread_spin_destroy(&spl);",
    summary: "A busy-wait lock — the thread spins in a tight loop instead of sleeping. Faster than a mutex for very short critical sections.",
    note: "Spinlocks waste CPU cycles while waiting and can cause priority inversion on preemptive systems. Only use when the critical section is shorter than a context-switch (~1-10 μs). For anything longer, a mutex that sleeps is better.",
    visual: { archetype: "criticalAtomic", params: { kind: "atomic" } },
    related: ["pt_mutex", "pt_trylock"],
  },
  {
    id: "pt_timedlock", tech: "pthreads", category: "Synchronization", name: "pthread_mutex_timedlock",
    signature: "struct timespec ts;\nclock_gettime(CLOCK_REALTIME, &ts);\nts.tv_sec += 5;   // 5 second timeout\nint rc = pthread_mutex_timedlock(&mtx, &ts);\nif (rc == ETIMEDOUT) { /* lock not acquired */ }",
    summary: "Try to acquire a mutex with a timeout — returns ETIMEDOUT if the lock isn't available within the deadline.",
    note: "Useful for deadlock detection: if a thread can't acquire a lock within a reasonable time, something is probably wrong. Also enables try-lock-or-skip patterns without busy-waiting.",
    visual: { archetype: "criticalAtomic", params: { kind: "critical" } },
    related: ["pt_mutex", "pt_trylock"],
  },
  {
    id: "pt_yield", tech: "pthreads", category: "Advanced", name: "sched_yield",
    signature: "#include <sched.h>\nsched_yield();   // voluntarily give up the CPU",
    summary: "Yield the CPU to another ready thread — a hint to the scheduler that this thread has nothing urgent to do right now.",
    note: "Rarely needed in well-structured parallel code. Useful in spin-wait loops to avoid wasting energy (while (!ready) sched_yield()), but a condition variable is almost always better.",
    visual: { archetype: "worksharing", params: { threads: 4 } },
    related: ["pt_cond", "pt_spinlock"],
  },

  /* ===================== CUDA ===================== */
  {
    id: "cuda_kernel", tech: "CUDA", category: "Kernel launch", name: "__global__ + <<<grid, block>>>",
    signature: "__global__ void kernel(float *d_a, int N) {\n  int i = blockIdx.x * blockDim.x + threadIdx.x;\n  if (i < N) d_a[i] *= 2.0f;\n}\n// host launch:\nkernel<<<(N+255)/256, 256>>>(d_a, N);",
    summary: "__global__ marks a function to run on the GPU; <<<grid, block>>> specifies how many thread blocks and threads per block to launch.",
    note: "Total threads = gridDim.x × blockDim.x. Always guard with if (i < N) when N isn't a multiple of blockDim.x. The launch is asynchronous — the host continues immediately.",
    visual: { archetype: "offload", params: { kind: "parallel" } },
    related: ["cuda_thread_idx", "cuda_dim3", "cuda_device_sync", "cuda_device_fn"],
  },
  {
    id: "cuda_thread_idx", tech: "CUDA", category: "Kernel launch", name: "threadIdx / blockIdx / blockDim / gridDim",
    signature: "// 1-D:\nint i = blockIdx.x * blockDim.x + threadIdx.x;\n// 2-D (e.g. matrix):\nint row = blockIdx.y * blockDim.y + threadIdx.y;\nint col = blockIdx.x * blockDim.x + threadIdx.x;",
    summary: "Built-in variables that give each thread its unique position — the GPU equivalent of omp_get_thread_num().",
    note: "threadIdx is 0-based within a block, blockIdx within the grid. For 2-D problems build row and col from the .y and .x components respectively.",
    visual: { archetype: "gangs", params: { level: "gang" } },
    related: ["cuda_kernel", "cuda_dim3"],
  },
  {
    id: "cuda_dim3", tech: "CUDA", category: "Kernel launch", name: "dim3 (grid / block dimensions)",
    signature: "dim3 block(16, 16);               // 16×16 = 256 threads/block\ndim3 grid((W+15)/16, (H+15)/16); // enough blocks to cover W×H\nkernel<<<grid, block>>>(d_a, W, H);",
    summary: "A 3-component integer vector for specifying grid and block dimensions — use for 2-D/3-D problems such as images, volumes, and matrices.",
    note: "Max blockDim is 1024 total threads (product of x·y·z). Max gridDim.x is ~2 billion; y and z are capped at 65535 on pre-Volta GPUs.",
    visual: { archetype: "gangs", params: { level: "gang" } },
    related: ["cuda_kernel", "cuda_thread_idx", "cuda_occupancy"],
  },
  {
    id: "cuda_device_fn", tech: "CUDA", category: "Kernel launch", name: "__device__ / __host__ __device__",
    signature: "__device__ float sq(float x) { return x*x; }  // GPU only\n\n__host__ __device__ float clamp01(float x) {\n  return fminf(1.f, fmaxf(0.f, x));\n}  // compiled for both CPU and GPU",
    summary: "__device__ compiles a function for the GPU only; __host__ __device__ emits versions for both — useful for math helpers shared between host and kernel code.",
    note: "A plain function (no qualifier) is __host__ only. Calling a host function from a kernel is a compile error. The dual qualifier is the cleanest pattern for portable math utilities.",
    visual: { archetype: "offload", params: { kind: "routine" } },
    related: ["cuda_kernel", "acc_routine"],
  },
  {
    id: "cuda_launch_bounds", tech: "CUDA", category: "Kernel launch", name: "__launch_bounds__",
    signature: "__global__ __launch_bounds__(256, 4)\nvoid kernel(float *d_a, int N) { ... }\n// 256 = max threads/block  4 = min resident blocks/SM",
    summary: "Hints to the compiler the maximum block size and desired min blocks-per-SM — enables more aggressive register allocation without spilling.",
    note: "If Nsight Compute reports high register spill traffic, reduce maxThreadsPerBlock here to free registers. Don't guess — profile first.",
    visual: { archetype: "gangs", params: { level: "vector" } },
    related: ["cuda_occupancy", "cuda_kernel"],
  },
  {
    id: "cuda_malloc", tech: "CUDA", category: "Memory management", name: "cudaMalloc / cudaFree",
    signature: "float *d_a;\ncudaMalloc(&d_a, N * sizeof(float));\n/* ... GPU work ... */\ncudaFree(d_a);",
    summary: "Allocate and free device (GPU) memory — the CUDA equivalent of malloc/free, but the pointer is only valid on the device.",
    note: "Always cudaFree when done to avoid device memory leaks. The returned pointer cannot be dereferenced on the host — doing so is undefined behaviour.",
    visual: { archetype: "accData", params: { clause: "create" } },
    related: ["cuda_memcpy", "cuda_malloc_managed", "cuda_memset", "cuda_error"],
  },
  {
    id: "cuda_memcpy", tech: "CUDA", category: "Memory management", name: "cudaMemcpy (H↔D)",
    signature: "// host → device (send input):\ncudaMemcpy(d_a, h_a, N*sizeof(float), cudaMemcpyHostToDevice);\n// device → host (fetch result):\ncudaMemcpy(h_a, d_a, N*sizeof(float), cudaMemcpyDeviceToHost);",
    summary: "Copy data between host (CPU) and device (GPU) memory — blocking: it implicitly synchronises the device before returning.",
    note: "Use cudaMemcpyAsync with a stream and pinned host memory to overlap copies with kernel execution. The synchronous form is safe but hides no latency.",
    visual: { archetype: "accData", params: { clause: "copy" } },
    related: ["cuda_malloc", "cuda_memcpy_async", "cuda_pinned", "cuda_stream"],
  },
  {
    id: "cuda_memset", tech: "CUDA", category: "Memory management", name: "cudaMemset",
    signature: "cudaMemset(d_a, 0, N * sizeof(float));  // zero the buffer\ncudaMemset(d_flags, -1, count);         // fill with 0xFF bytes",
    summary: "Fill device memory with a byte pattern — the GPU equivalent of memset for initialising device arrays.",
    note: "Sets bytes, not values: cudaMemset(p, 1, n) fills with byte 0x01, not the integer 1. For arbitrary float/int values, write a small init kernel or use cudaMemcpyAsync from a prefilled host buffer.",
    visual: { archetype: "accData", params: { clause: "create" } },
    related: ["cuda_malloc", "cuda_memcpy"],
  },
  {
    id: "cuda_malloc_managed", tech: "CUDA", category: "Memory management", name: "cudaMallocManaged (unified memory)",
    signature: "float *a;\ncudaMallocManaged(&a, N * sizeof(float));\nkernel<<<grid, block>>>(a, N);\ncudaDeviceSynchronize();  // must sync before host access\nprintf(\"%f\", a[0]);       // valid on host after sync\ncudaFree(a);",
    summary: "Allocates unified memory — one pointer valid on both CPU and GPU; the driver migrates pages on demand.",
    note: "Great for prototyping; page-fault migration adds overhead in production. Use cudaMemPrefetchAsync to move pages before the kernel touches them and recover most of that cost.",
    visual: { archetype: "accData", params: { clause: "copy" } },
    related: ["cuda_malloc", "cuda_memcpy", "cuda_device_sync"],
  },
  {
    id: "cuda_pinned", tech: "CUDA", category: "Memory management", name: "cudaMallocHost (pinned memory)",
    signature: "float *h_a;\ncudaMallocHost(&h_a, N * sizeof(float));  // page-locked\ncudaMemcpyAsync(d_a, h_a, bytes,\n  cudaMemcpyHostToDevice, stream);          // truly async\n/* ... compute ... */\ncudaFreeHost(h_a);",
    summary: "Page-locked (pinned) host memory enables full-speed PCIe DMA and makes cudaMemcpyAsync truly non-blocking.",
    note: "Pinned memory is not pageable, so the OS can't swap it out — over-allocating it degrades system memory. Pin only large, frequently transferred buffers and free with cudaFreeHost.",
    visual: { archetype: "accData", params: { clause: "copyin" } },
    related: ["cuda_memcpy_async", "cuda_stream", "cuda_memcpy"],
  },
  {
    id: "cuda_memcpy_async", tech: "CUDA", category: "Memory management", name: "cudaMemcpyAsync",
    signature: "cudaMemcpyAsync(d_a, h_a, bytes,\n  cudaMemcpyHostToDevice, stream);\nkernel<<<grid, block, 0, stream>>>(d_a, N);\ncudaMemcpyAsync(h_out, d_out, bytes,\n  cudaMemcpyDeviceToHost, stream);\ncudaStreamSynchronize(stream);",
    summary: "Non-blocking memory transfer — returns immediately and lets the CPU keep running or overlap with work on another stream.",
    note: "Requires pinned host memory (cudaMallocHost) to be truly async; pageable buffers fall back to synchronous. Chain copy→kernel→copy on one stream for simple overlap; use two streams to pipeline across iterations.",
    visual: { archetype: "accData", params: { clause: "copy" } },
    related: ["cuda_stream", "cuda_pinned", "cuda_event"],
  },
  {
    id: "cuda_shared", tech: "CUDA", category: "Shared memory", name: "__shared__",
    signature: "__global__ void kernel(float *d_a) {\n  __shared__ float tile[256];  // on-chip, shared by block\n  tile[threadIdx.x] = d_a[blockIdx.x*blockDim.x + threadIdx.x];\n  __syncthreads();             // fence before cross-thread read\n  float v = tile[(threadIdx.x+1) % blockDim.x]; /* ... */\n}",
    summary: "On-chip shared memory, shared by all threads in a block — ~100× lower latency than global DRAM and the primary tool for data reuse inside a block.",
    note: "Limited to ~48–96 KB per SM, split across all resident blocks. Access patterns matter: strided access causes bank conflicts (32-bank, 4-byte interleaved). Always __syncthreads() after writing before reading across threads.",
    visual: { archetype: "dataSharing", params: { clause: "shared" } },
    related: ["cuda_syncthreads", "cuda_bank_conflicts", "cuda_kernel"],
  },
  {
    id: "cuda_bank_conflicts", tech: "CUDA", category: "Shared memory", name: "Bank conflicts",
    signature: "// 32 banks, 4-byte wide (stride-1 = conflict-free):\ntile[threadIdx.x] += 1;          // GOOD: each thread → own bank\n// stride-32 = 32-way conflict (all threads → bank 0):\ntile[threadIdx.x * 32] += 1;    // BAD\n// fix: pad by 1 element per row\n__shared__ float t[NUM_ROWS][32 + 1];",
    summary: "Shared memory is divided into 32 banks; simultaneous accesses to the same bank serialise — a bank conflict.",
    note: "Stride-1 (adjacent addresses) is conflict-free. Stride-32 is the worst case: 32-way serialisation and 32× slowdown. Padding each shared-memory row by one element breaks repeated-stride patterns in 2-D tiles.",
    visual: { archetype: "dataSharing", params: { clause: "shared" } },
    related: ["cuda_shared", "cuda_syncthreads", "cuda_coalescing"],
  },
  {
    id: "cuda_syncthreads", tech: "CUDA", category: "Synchronization", name: "__syncthreads()",
    signature: "tile[threadIdx.x] = d_a[i];      // write\n__syncthreads();                  // ALL threads in block reach here\nfloat v = tile[(threadIdx.x+1) % blockDim.x]; // safe read",
    summary: "A barrier for all threads in a block — no thread proceeds until every thread has reached this call.",
    note: "__syncthreads() synchronises within a block only. There is no built-in cross-block barrier inside a kernel — end the kernel and call cudaDeviceSynchronize() on the host instead.",
    visual: { archetype: "barrier", params: { kind: "barrier" } },
    related: ["cuda_shared", "cuda_device_sync", "cuda_syncwarp"],
  },
  {
    id: "cuda_device_sync", tech: "CUDA", category: "Synchronization", name: "cudaDeviceSynchronize()",
    signature: "kernel<<<grid, block>>>(d_a, N);\ncudaDeviceSynchronize();   // host blocks until GPU done\ncudaMemcpy(h_a, d_a, ..., cudaMemcpyDeviceToHost); // safe",
    summary: "Blocks the calling CPU thread until all previously issued GPU work on the device completes.",
    note: "The blocking cudaMemcpy implies this; you don't need an extra cudaDeviceSynchronize before it. For finer control — waiting on one stream only — use cudaStreamSynchronize(stream) or cudaEventSynchronize(event).",
    visual: { archetype: "barrier", params: { kind: "barrier" } },
    related: ["cuda_syncthreads", "cuda_stream", "cuda_event"],
  },
  {
    id: "cuda_atomics", tech: "CUDA", category: "Synchronization", name: "atomicAdd / atomicCAS / atomicMax",
    signature: "atomicAdd(&d_count, 1);            // fetch-and-add (int or float)\natomicMax(&d_max, local_val);      // update only if val > *d_max\nint old = atomicCAS(&d_lock, 0, 1); // compare-and-swap (mutex)",
    summary: "Hardware-atomic read-modify-write on global or shared memory — accumulate values across threads without races.",
    note: "Contention on a single global address serialises warps and kills throughput. Reduce per warp with __shfl_down_sync first, then one atomicAdd per warp — 32× less contention.",
    visual: { archetype: "criticalAtomic", params: { kind: "atomic" } },
    related: ["cuda_shfl", "cuda_shared", "omp_atomic"],
  },
  {
    id: "cuda_syncwarp", tech: "CUDA", category: "Synchronization", name: "__syncwarp / warp voting",
    signature: "__syncwarp();                // barrier for the 32-thread warp\n__ballot_sync(0xffffffff, pred); // bitmask: which lanes have pred\n__all_sync(0xffffffff, pred);    // true iff ALL active lanes do\n__any_sync(0xffffffff, pred);    // true iff ANY active lane does",
    summary: "Warp-scope sync and voting — cheaper than __syncthreads() because they operate on the 32 threads that physically execute together.",
    note: "Required for any intra-warp shared-memory communication on Volta+ (SM 7.0+) because independent thread scheduling means the warp can now diverge inside a block.",
    visual: { archetype: "barrier", params: { kind: "barrier" } },
    related: ["cuda_shfl", "cuda_syncthreads"],
  },
  {
    id: "cuda_shfl", tech: "CUDA", category: "Warp primitives", name: "__shfl_down_sync (warp reduction)",
    signature: "__device__ float warpReduceSum(float val) {\n  for (int d = 16; d > 0; d >>= 1)\n    val += __shfl_down_sync(0xffffffff, val, d);\n  return val;  // lane 0 holds the sum\n}",
    summary: "Warp-level shuffle: a thread reads another thread's register directly — no shared memory needed, zero bank conflicts, ~4× faster than a shared-memory reduction.",
    note: "All 32 lanes must participate (active mask 0xffffffff). Result lands on lane 0; broadcast with __shfl_sync if all lanes need it. Requires CUDA 9+ and SM ≥ 7.0.",
    visual: { archetype: "reduction", params: { threads: 4 } },
    related: ["cuda_atomics", "cuda_shared", "cuda_syncwarp"],
  },
  {
    id: "cuda_stream", tech: "CUDA", category: "Concurrency", name: "cudaStream_t",
    signature: "cudaStream_t s1, s2;\ncudaStreamCreate(&s1); cudaStreamCreate(&s2);\nkernelA<<<grid, block, 0, s1>>>(d_a, N);\ncudaMemcpyAsync(d_b, h_b, bytes, cudaMemcpyHostToDevice, s2);\ncudaStreamSynchronize(s1);\ncudaStreamDestroy(s1); cudaStreamDestroy(s2);",
    summary: "A stream is a GPU work queue — operations in different streams can execute concurrently, enabling compute+transfer overlap.",
    note: "The default stream (0) serialises with all other streams. Create non-NULL streams to get real concurrency. Use cudaStreamWaitEvent to express cross-stream dependencies.",
    visual: { archetype: "forkJoin", params: { threads: 3 } },
    related: ["cuda_event", "cuda_memcpy_async", "cuda_device_sync"],
  },
  {
    id: "cuda_event", tech: "CUDA", category: "Concurrency", name: "cudaEvent_t",
    signature: "cudaEvent_t t0, t1;\ncudaEventCreate(&t0); cudaEventCreate(&t1);\ncudaEventRecord(t0);\nkernel<<<grid, block>>>(d_a, N);\ncudaEventRecord(t1);\ncudaEventSynchronize(t1);\nfloat ms; cudaEventElapsedTime(&ms, t0, t1);\nprintf(\"%.3f ms\\n\", ms);",
    summary: "GPU timestamps and synchronisation points — the correct way to time kernels and express cross-stream dependencies.",
    note: "CPU timers (omp_get_wtime, clock()) measure host time, not GPU time. cudaEventElapsedTime gives the true kernel duration. Use cudaStreamWaitEvent(stream, event) to make one stream wait for a point in another.",
    visual: { archetype: "barrier", params: { kind: "barrier" } },
    related: ["cuda_stream", "cuda_device_sync"],
  },
  {
    id: "cuda_constant", tech: "CUDA", category: "Memory model", name: "__constant__",
    signature: "__constant__ float c_weights[256];  // 64 KB broadcast cache\n// host upload (before kernel):\ncudaMemcpyToSymbol(c_weights, h_weights, 256*sizeof(float));\n// kernel: read c_weights[i] — cached, broadcast to all threads",
    summary: "A 64 KB GPU region that is cached and broadcast to all threads reading the same address simultaneously — ideal for lookup tables and filter kernels.",
    note: "Uniform access (all threads read the same index) is fast. Divergent access (each thread reads a different index) serialises through the cache and erases the benefit.",
    visual: { archetype: "dataSharing", params: { clause: "shared" } },
    related: ["cuda_shared", "cuda_memcpy", "cuda_coalescing"],
  },
  {
    id: "cuda_coalescing", tech: "CUDA", category: "Memory model", name: "Memory coalescing",
    signature: "// COALESCED: 32 threads → 32 consecutive floats (1 transaction)\nd_a[blockIdx.x * blockDim.x + threadIdx.x] = val;\n\n// UNCOALESCED: stride-N (up to 32 transactions, N× slower)\nd_a[(blockIdx.x * blockDim.x + threadIdx.x) * stride] = val;",
    summary: "A warp's 32 threads coalesce their global memory accesses into one transaction when they touch consecutive addresses — non-coalesced access multiplies DRAM traffic by up to 32×.",
    note: "Prefer SoA (struct of arrays) layout over AoS (array of structs) for GPU data: SoA gives each thread a consecutive element; AoS gives strided accesses across fields. Profile with Nsight Compute's 'Global Memory Access Pattern' metric.",
    visual: { archetype: "accData", params: { clause: "copy" } },
    related: ["cuda_shared", "cuda_bank_conflicts", "cuda_constant"],
  },
  {
    id: "cuda_occupancy", tech: "CUDA", category: "Performance", name: "cudaOccupancyMaxPotentialBlockSize",
    signature: "int minGrid, block;\ncudaOccupancyMaxPotentialBlockSize(\n  &minGrid, &block, myKernel, 0, 0);\nint grid = (N + block - 1) / block;\nmyKernel<<<grid, block>>>(d_a, N);",
    summary: "Asks the runtime for the block size that maximises SM occupancy for a given kernel — removes the guesswork from the launch configuration.",
    note: "High occupancy helps hide memory latency by keeping the warp scheduler busy, but it isn't always fastest (it can hurt L1 cache and register pressure). Profile first.",
    visual: { archetype: "gangs", params: { level: "gang" } },
    related: ["cuda_kernel", "cuda_dim3", "cuda_device_prop", "cuda_launch_bounds"],
  },
  {
    id: "cuda_device_prop", tech: "CUDA", category: "Performance", name: "cudaGetDeviceProperties",
    signature: "cudaDeviceProp p;\ncudaGetDeviceProperties(&p, 0);\nprintf(\"%s  SM %d.%d  %d SMs  %zu MB\\n\",\n  p.name, p.major, p.minor,\n  p.multiProcessorCount, p.totalGlobalMem>>20);\n// also: p.sharedMemPerBlock, p.warpSize, p.maxThreadsPerBlock",
    summary: "Query hardware specs at runtime: device name, compute capability, SM count, total memory, shared memory per block, warp size, max threads.",
    note: "Always gate features (warp primitives, tensor cores, cooperative groups) on the compute capability you query here — never hardcode SM ≥ 7.0 assumptions.",
    visual: { archetype: "offload", params: { kind: "parallel" } },
    related: ["cuda_occupancy", "cuda_kernel", "cuda_visible_devices"],
  },
  {
    id: "cuda_error", tech: "CUDA", category: "Performance", name: "cudaGetLastError / CUDA_CHECK macro",
    signature: "// after a kernel:\ncudaError_t e = cudaGetLastError();\nif (e != cudaSuccess)\n  fprintf(stderr, \"%s\\n\", cudaGetErrorString(e));\n// production pattern:\n#define CUDA_CHECK(c) do { cudaError_t _e=(c); \\\n  if(_e!=cudaSuccess){fprintf(stderr,\"%s %s:%d\\n\", \\\n  cudaGetErrorString(_e),__FILE__,__LINE__);exit(1);}}while(0)",
    summary: "Kernel launches are asynchronous — cudaGetLastError catches launch failures (bad grid/block, out of resources) after the fact.",
    note: "Wrap every API call with a CUDA_CHECK macro. For errors inside the kernel body (illegal memory access, stack overflow), call cudaDeviceSynchronize() then cudaGetLastError() to flush them to the host.",
    visual: { archetype: "ranksMemory", params: { threads: 4 } },
    related: ["cuda_kernel", "cuda_device_sync", "cuda_malloc"],
  },
  {
    id: "cuda_visible_devices", tech: "CUDA", category: "Performance", name: "CUDA_VISIBLE_DEVICES",
    signature: "export CUDA_VISIBLE_DEVICES=0        # GPU 0 only\nexport CUDA_VISIBLE_DEVICES=0,2      # GPUs 0 and 2 (renumbered 0,1)\nexport CUDA_VISIBLE_DEVICES=\"\"        # hide all GPUs\n# Slurm sets this automatically with --gres=gpu:N",
    summary: "Controls which physical GPUs a CUDA program sees — device numbering inside the program follows the list order.",
    note: "Never hardcode physical GPU ids in cluster jobs; Slurm sets CUDA_VISIBLE_DEVICES so device 0 inside your process is always the allocated GPU.",
    visual: { archetype: "offload", params: { kind: "parallel" } },
    related: ["slurm_gpu", "cuda_device_prop"],
  },
];

export const TECHS: Tech[] = ["OpenMP", "MPI", "OpenACC", "Slurm", "pthreads", "CUDA"];
export const categoriesFor = (tech: Tech) =>
  Array.from(new Set(ENTRIES.filter((e) => e.tech === tech).map((e) => e.category)));

// The must-know directives a newcomer actually needs first — powers the /reference
// "Essentials" filter so the ~200-entry library isn't overwhelming on day one.
export const ESSENTIAL = new Set<string>([
  // OpenMP
  "omp_parallel", "omp_parallel_for", "omp_for", "omp_reduction", "omp_critical", "omp_atomic",
  "omp_barrier", "omp_private", "omp_shared", "omp_schedule_dynamic", "omp_get_thread_num",
  // MPI
  "mpi_init", "mpi_comm_rank", "mpi_comm_size", "mpi_send", "mpi_recv", "mpi_bcast",
  "mpi_scatter", "mpi_gather", "mpi_reduce", "mpi_allreduce", "mpi_barrier",
  // OpenACC
  "acc_parallel_loop", "acc_kernels", "acc_data", "acc_copyin",
  // Slurm
  "slurm_sbatch", "slurm_srun", "slurm_squeue", "slurm_scancel", "slurm_nodes", "slurm_gpu",
  "slurm_sinfo", "slurm_mem", "slurm_time",
  // pthreads
  "pt_create", "pt_join", "pt_mutex", "pt_cond", "pt_barrier", "pt_spinlock",
  // CUDA
  "cuda_kernel", "cuda_thread_idx", "cuda_malloc", "cuda_memcpy",
  "cuda_shared", "cuda_syncthreads", "cuda_device_sync", "cuda_atomics",
  "cuda_stream", "cuda_error", "cuda_coalescing",
]);
